# Dot Rigger 작업 내역 — 2026-09-22

> 커밋용 요약. 앞의 것은 `작업내역_2026-09-21.md`. 자세한 기록은 `Docs/인수인계/01 §4-27 · 02 U15`, 사용법은 `addons/dot_rigger/README.md` "장비(무기·헬멧) 굽기 창".

## 한 줄 요약
**장비(무기·헬멧) 굽기 창** — `프로젝트 > 도구 > Dot Rigger — 장비 굽기 (무기·헬멧)`. 무기 3D 를 불러 자동 그립 → **2D 미리보기 위의 기즈모**로 조정(공통 + 자세별) → 총의 기본 자리 + **앞 조각 자동**(총을 쥔 손가락이 총 앞에) → 모든 자세에 맞춰 굽기 → `equip.json` 으로 다시 열어 고치기.

## 어디서 조작하나

| 하고 싶은 것 | 위치 |
|---|---|
| 무기 불러오기 | 왼쪽 `1. 캐릭터 · 세트 · 무기` — 무기 모델의 `…` 로 고르면 바로 불러온다 |
| 탄 클립 등 안 찍기 | `2. 무기 파트` 체크 끄기 |
| **피벗(그립) 맞추기** | 불러오면 자동으로 잡힌다(`손이 잡는 자리` 는 **방아쇠 노드**로). 그 위에 **오른쪽 2D 의 기즈모**: 빨강 = 좌우, 초록 = 위아래, 가운데 점 = 자유 이동, 파란 링 = 기준 손 중심 회전, Shift = 1/10. `3. 잡는 법` 의 `붙일 손 기준`/`받치는 손 기준` 이 기즈모 자리와 회전 중심, `이 자세만` 이면 그 자세에만, `조정 리셋` |
| 팔·손가락 앞뒤 | `4. 총의 기본 자리 · 앞 조각` — 기본 자리(`Torso 바로 앞`)만 고르면 그 뒤 파트 중 무기보다 앞인 부분(왼손 손가락 등)은 **앞 조각**으로 자동. 자세마다 어떤 파트가 몇 px 됐는지 아래에 |
| 확인 | 오른쪽 2D(실제 퍼펫 + 임시로 구운 무기 + 앞 조각, 자세 · 동작 · 재생 · 조준 · 확대). `3D 보기` 는 깊이 확인용 |
| 굽기 | 바닥 `굽기 (모든 자세)` → `<출력 폴더>/<장비 이름>/` PNG + 앞 조각 PNG + equip.json |
| 다시 고치기 | `구운 장비 열기…` → equip.json |

⚠ 새 클래스(`DREquipWindow`) + `plugin.gd` 변경 → **Godot 재시작** 후 메뉴에 나온다. 09-22 에 두 번 바뀌었으므로(창 → 기즈모·앞 조각) 어제 창을 봤어도 다시 재시작.

⚠ **지금 `puppet/` 세트는 09-21 이전 방식(파트 z 간격 1)이라** 무기 자리가 정확히 안 보인다 — 2D 미리보기 아래에 경고가 뜬다. 메인 창에서 세트를 다시 구우면(그 뒤 장비도 다시 굽기) 해결.

## 바뀐 것

| # | 내용 | 왜 | 기록 |
|---|---|---|---|
| 1 | **`DREquipWindow`**(`ui/equip_window.gd`) — 자기 DRBaker(프리셋에서 setup) · 무기 파트 체크 · 자동 그립 · 그리기 자리(공통/자세별) · 2D 미리보기(실제 `DRPuppetSet` 에 임시로 구운 무기 장착, 재생·조준) · 굽기 · `구운 장비 열기…`(복원) | 사용자 "역시 툴 없이 만드니 피벗이 안 맞아 — 장비창 툴을 만들어야겠어" | 01 §4-27 |
| 2 | **2D 기즈모** — 이동 화살표 2 + 회전 링, `붙일 손 기준`/`받치는 손 기준`(기즈모 자리 = 회전 중심), `이 자세만`, `조정 리셋`, Shift 미세. 끄는 동안 그림이 바로 따라오고 놓으면 조정값으로 되구해(`nudge_to_adjust`/`solve_adjust`) 다시 굽는다. 3D 미리보기는 기본 숨김. `잡는 자리 옮기기` XYZ 칸 삭제 | 사용자 1~4: "3D 자세 뷰 필요 없음 · 2D 평면 좌표계 기즈모로 · 왼손/오른손 기준 토글 · 리셋 · XYZ 입력 필드로 치지 말자 · 한 축은 없어도 됨" | 01 §4-27 ⑤ |
| 3 | **앞 조각 자동 굽기** — 무기는 기본 자리 한 곳, 그 뒤 파트 중 3D 에서 무기보다 앞인 픽셀만 따로 찍어 파트에 붙여 무기 z + 1 에(`DREquipItem.overlays` · `DRPuppet.equip()` · `equip.json` `sets[].overlays` · 파일 `<세트>_front_<파트>.png`). 파트 ID 렌더(색상환 색 + 무기 흰색)로 픽셀마다 맨 앞 판정 | 사용자 5~7: "X 바로 앞 한 자리로는 무기가 왼손 위에 온다 — 장비가 파츠를 개별로 이해해야" → 순환이라 맞는 지적. Spine 의 손 앞/뒤 나누기를 자동화하기로 합의("기본 소트는 메인 툴, 보정은 장비 툴") | 01 §4-27 ④ |
| 4 | **`DREquipBaker` 개편** — 조정 모델(`grip_base` + `grip_frame`(무기 축) + `grip_pivot` + `adj_pos/adj_rot` + `set_adjust`), `compose_grip/solve_adjust/nudge_to_adjust`, `bake_set/bake_all`(메모리, 앞 조각 · 손바닥 캔버스 좌표 포함), `build_equip_set`, `parts_behind_weapon`, `view_hand`, `to_dict/from_dict/load_json`(equip.json v2), Mesh 로 가져온 무기도 받음 | 창과 CLI 가 같은 코어 | 01 §4-27 |
| 5 | `DRPuppetSet.run_in_editor` · `speed_scale` / `DREquipSet.load_json` 세트별 자리 + 앞 조각 / `DRPuppet.get_equipped_overlays` / `plugin.gd` 메뉴 / `equip_bake_cli` 새 API | — | — |
| 6 | 새 검사 `equip_window_smoke_cli` (8단계) | — | 인수인계 README ⑭ |
| 7 | 버그 — 복원 뒤 `material is null` ×8: 같은 프레임에 옛 무기 `queue_free` + 같은 이름으로 다시 만들어 옛 것이 안 지워짐 → `remove_weapon` 을 즉시 `free()` 로 | 스모크 [8] 에서 발견 | 02 U15 |
| 8 | 앞 조각이 0 이 되는 경우 — `손이 잡는 자리` 를 경계 상자 어림으로 두면 총이 왼손 중심을 지나 손보다 앞에 놓인다 → 방아쇠 노드를 고르면 L_Hand 126px | 스모크 [3] 에서 발견 | 01 §4-27 ④ |

## 검증 (옛 사본 `Documents\1941` — 코드 동일, 사용자의 실제 `puppet/` 세트 3개 + kar98k)

| 도구 | 결과 |
|---|---|
| `equip_window_smoke_cli` | 전부 통과 — 앞 조각 L_Hand 126px·Torso 8px(무기 z + 1, 파트 뼈에) · 기즈모 10px → 10px · 15° → 15° · 붙일 손/받치는 손 자리 0.0000m 제자리 · 드래그 흉내 · 이 자세만 · 3D 보기 · 굽기(앞 조각 파일 2) · 다시 열기 복원 |
| `equip_bake_cli`(새 API) · `puppet_set_check` · `layer_check` · `composite_check` · `sets_check` | 전부 통과 |
| 실제 프로젝트 | 코드 동일 복사(`.uid` 포함), 에디터가 꺼진 틈에 `--import` 로 클래스 등록. **에디터에서 창을 직접 열어 본 건 아직 아님**(`가져오기 형식을 Scene 으로` 버튼은 에디터 전용이라 미검증) |

그림: `Docs/인수인계/img/equip_window.png`(창 — 2D 미리보기 위 기즈모) · `equip_kar98k_aim.png`.

## 파일
- 새: `addons/dot_rigger/ui/equip_window.gd` · `tools/equip_window_smoke_cli.gd` (+ `.uid`) · 이 문서 · `Docs/인수인계/img/equip_window.png`
- 고침: `core/equip_baker.gd`(개편) · `runtime/dr_puppet.gd` · `runtime/dr_puppet_set.gd` · `runtime/dr_equip_set.gd` · `runtime/dr_equip_item.gd` · `plugin.gd` · `tools/equip_bake_cli.gd` · 문서 4곳
- `equip/kar98k/` 를 새 형식(v2 + 앞 조각)으로 다시 구움(.gitignore 라 저장소엔 없음)
