# Dot Rigger 작업 내역 — 2026-09-21

> 커밋용 요약. 앞의 것은 `작업내역_2026-09-18.md`. 자세한 기록은 `Docs/인수인계/`(01 §4-23 · §4-24 · §4-25 · 02 U13 · M4), 사용법은 `addons/dot_rigger/README.md`.

## 한 줄 요약
**① 픽셀 퍼펙트**(상단 바 — 선의 색을 `카툰` / `픽셀 퍼펙트` 중 하나로. 픽셀 퍼펙트 = 선의 도트마다 바로 옆 몸 도트 색을 어둡게) **② 상하체 합성**(5번의 `상하체 합성…` 버튼 → 팝업에서 "하체 동작 + 상체 동작 = 새 동작") **+ 도트 격자 고정**(처음에 픽셀 퍼펙트로 잘못 알아듣고 만든 것 — 이름을 바꿔 남김).

## 어디서 조작하나

| 기능 | 위치 |
|---|---|
| **픽셀 퍼펙트** | 프리뷰 **위쪽 상단 바** — `아웃라인 [두께]` 옆의 **`카툰` ○ / `픽셀 퍼펙트` ○**(둘 중 하나만 켜짐). 두께를 1 이상으로 두고 `픽셀 퍼펙트` 를 켜면 선이 옆 몸 색의 짙은 톤으로 들어간다. 옆의 `%` = 어둡게 하는 정도(기본 45). `2D 순서` 를 켜야 프리뷰에서 보인다 |
| **상하체 합성** | 왼쪽 열 **`5. 리깅 애니메이션`** → 동작 목록 아래 `선택 N개 · 선택 비우기` 줄 **바로 밑의 `상하체 합성…` 버튼** → 팝업. (왼쪽 열을 조금 내려야 보인다. `레스트 자동` 체크박스 바로 위) |
| **도트 격자 고정** | 프리뷰 **위쪽 상단 바** — `아웃라인` · `전체 실루엣` 옆의 **`도트 격자 고정`** 체크박스. `2D 순서` 를 켜야 프리뷰에서 보인다 |

그림: `Docs/인수인계/img/composite_keep_compare.png`(Crouch_Idle + Idle — 왼쪽부터 방향 유지 0% · 50% · **100%** · 100% + 보정 −10°) · `outline_style_compare.png`(왼쪽부터 선 없음 · 카툰 · **픽셀 퍼펙트 45%** · 픽셀 퍼펙트 25%) · `composite_button.png`(버튼 자리) · `composite_dialog.png`(팝업) · `composite_crouch_aim_walk.png`(합성 결과, 위 3D · 아래 2D) · `pixel_compare.png`(왼쪽 도트 격자 고정 · 오른쪽 그냥 3배). 그 폴더는 `.gdignore` 로 Godot 임포트에서 뺐다.

⚠ 버튼이 안 보이면 예전 창이 그대로 떠 있는 것이다 — **Godot 재시작** 또는 `프로젝트 > 도구 > Dot Rigger — 창 새로 만들기 (초기화)`.

### 상하체 합성 쓰는 순서
1. 모델을 불러온다(소총 동작을 쓰려면 모델 `res://source3d/UAL1_humanoid.glb` + 추가 동작 폴더 `res://source3d/mixamo`).
2. `상하체 합성…` → 팝업에서 `추가`.
3. **하체 동작**(예: `Crouch_Fwd`) · **상체 동작**(예: `Rifle_Aiming_Idle`)을 고른다. 옆의 `검색` 칸에 글자를 넣으면 목록이 줄어든다.
4. **이름**을 넣는다(예: `Crouch_Aim_Walk`) — 게임에서 이 이름으로 재생한다. 비우면 `하체+상체`.
5. **길이 맞춤**: 걷기·뛰기가 하체면 `하체 시간에 상체를 맞춤 (하체 반복)`(기본), 사격·장전 속도가 중요하면 `상체 시간에 하체를 맞춤 (하체 반복)`. `(하체 1번)` 은 둘 다 한 번 하고 끝나는 동작을 같이 끝낼 때. 아래에 몇 초짜리가 되는지 미리 나오고, **오른쪽 프리뷰에서 바로 재생**된다.
6. 왼쪽 **상체로 칠 파트**는 보통 그대로(몸통·머리·두 팔).
7. `적용` → 5번 동작 목록에 **하늘색**으로 나타나고 자동으로 골라진다. 그 뒤는 다른 애니와 똑같다 — ▶ 재생으로 보고, 6번 세트에 담고, 베이크.

## 바뀐 것

| # | 내용 | 왜 | 기록 |
|---|---|---|---|
| 1 | **상하체 합성** — `DRBaker.build_composites()`: 상체 파트의 뼈 트랙은 상체 애니에서, 힙·다리는 하체 애니에서 가져와 새 동작을 만든다(3D 뼈대 단계, 메모리에서만). 길이 맞춤 3종(하체 속도 유지 / 한 번씩 / 상체 속도 유지), 반복 여부 | 사용자 요청 "상하체 분리하여 합성해서 애니메이션 세트에 넣는 기능". 라이브러리에 "앉아 걸으며 조준" 같은 전신 동작이 없다 | 01 §4-23 |
| 2 | **팝업 `DRCompositeDialog`**(`ui/composite_dialog.gd`) — 상체 파트 체크 15칸 · 합성 목록(추가·복제·삭제·▲▼) · 편집 칸 · 길이 미리보기 · `적용`. 5번의 `상하체 합성…` 버튼(합성 수 표시). 만든 동작은 목록에 하늘색 + 툴팁 | 사용자 요청 "상하체 분리는 에디터 팝업으로 설정" | 01 §4-23 |
| 2-1 | **팝업 안 프리뷰** — 오른쪽에 고른 합성의 결과가 **`적용` 전에도 그 자리에서 재생**된다(▶ 재생 · 속도 ×1/½/¼ · 위치 슬라이더). 하체·상체 동작, 길이 맞춤, 상체 파트를 바꾸면 바로 다시 만들어진다. 팝업이 떠 있는 동안 창의 재생은 멈춤. 미리보기 동작은 숨은 이름이라 목록·세트·베이크에 안 나오고 닫으면 치워진다 | 사용자 요청 "팝업에 합체된 결과물 프리뷰가 떠 있고 여기서도 재생" | 01 §4-23 |
| 2-2 | **길이 맞춤 4가지 = 누구의 시간에 맞추나 × 하체를 몇 번 돌리나** — `하체 시간에 상체를 맞춤 (하체 반복)`(기본) · `하체 시간에 상체를 맞춤 (하체 1번)` · `상체 시간에 하체를 맞춤 (하체 반복)` · `상체 시간에 하체를 맞춤 (하체 1번)`. 마지막 것이 새로 추가(값 3). 값(0~3)은 그대로라 저장된 프리셋은 안 바뀜 | 사용자 "상체에 맞춤은 없어?"(있었는데 이름에서 안 읽힘) → "한 번씩이 무슨 뜻인지 모르겠다" → 사용자가 풀어 준 말 "하체의 초에 상체를 맞춘다" 를 그대로 이름으로 | 01 §4-23 |
| 2-3 | **`방향 유지`(0~100%) · `각도 보정`(도)** — 합성마다. 앉기처럼 골반이 숙는 하체 위에 얹은 상체가 같이 숙는 것을 막는다. 100% = 몸통이 상체 동작에서 보던 방향 그대로, 각도 보정 + = 뒤로 젖힘 · − = 앞으로 숙임. 프리뷰에 바로 보임. 새로 추가한 합성은 100%, **칸이 생기기 전에 만든 합성은 0% 로 열리니 직접 올릴 것** | 사용자 "하체는 앉고 상체는 Idle 인데 상체가 대각선 아래를 본다 · Torso 를 끄면 더 숙는다 → 두 방식 다 옵션으로". 재 보니 UAL Crouch_Idle 은 골반 27° · 몸통 49° 숙은 잠입 자세 | 01 §4-23 · 02 M5 |
| 2-4 | **버그 수정 — 합성의 각도 보정이 그냥 Idle 에까지 묻어남**. 애니를 세우기 전에 뼈대를 레스트로 되돌린다(`DRBaker.set_pose`). 값을 공용으로 쓴 게 아니라, 뼈대 하나를 모든 애니가 돌려 쓰는데 Idle 에는 허리 첫 마디의 트랙이 없어서 앞서 본 합성의 자세가 그 뼈에 남았던 것(가슴 57° 차이 → 0.000°). **같은 원인으로 09-18 이후 Mixamo 동작을 본 직후의 UAL 동작도 허리가 6~16° 돌아가 있었다** → Mixamo 와 같이 구운 결과는 다시 구울 것(UAL 끼리만 구운 `puppet/` 은 영향 없음) | 사용자 "팝업의 각도 조절값이 그냥 IDLE 에서 관여한다" | 02 C13 |
| 2-5 | **버그 수정 — 검색해서 고른 동작에는 레스트 자동이 안 따라옴**. 5번 목록: **그냥 클릭 = 그것만**(검색으로 가려진 선택까지 풀림) · **Ctrl/Shift + 클릭 = 추가**(가려진 선택 유지 — 검색어를 바꿔 가며 모을 때). 검색어를 바꾸는 것 자체는 예전처럼 선택을 안 건드림 | 사용자 "목록을 클릭하면 레스트 자동이 반영되는데 검색해서 찾을 때는 안 된다" — 가려진 선택이 남아 클릭이 '추가' 가 되고, 레스트 자동은 고른 것 전체에서 찾으니 방금 누른 동작을 안 따라갔다 | 02 U14 |
| 2-6 | **런타임 `DRPuppetSet`**(`runtime/dr_puppet_set.gd`) — `sets.json` 을 읽어 세트(폴더별 `puppet.tscn`) 여러 벌을 **캐릭터 하나처럼**: `play("동작 이름")` 하면 그 동작이 든 세트로 알아서 바꾼다 · 세트가 바뀌어도 발 자리가 같다 · **마우스 조준**(`aim_enabled`·`aim_target` — 애니 위에 몸통 각도를 더함, 한계 `aim_limit_deg`, 등 뒤면 좌우 반전). **시험장 `res://test/puppet_test.tscn`**(열고 F6: 숫자 = 동작, 마우스 = 조준) | 사용자(Unity 출신) "애니마다 puppet 씬이 폴더별로 나뉘어 어떻게 관리할지 모르겠다 · 조준 상태에서 마우스를 따라 겨누는 걸 런타임에서 보고 싶다" | 01 §4-26 |
| 2-7 | **세트 굽기가 옛 세트 폴더를 알아서 치운다**(`DRSetBaker._clean_stale`) — 지난번 `sets.json` 에 있었고 이번 목록에 없는 폴더만, 툴이 만든 파일만(손으로 넣은 파일이 있으면 남기고 알림). 세트 이름의 대소문자만 바꿔도 안전(디스크 폴더 이름도 맞춘다). 완료 글에 치운 폴더가 나온다 | 사용자 "(옛 폴더 지우기)는 니가 해줘" — 세트를 다시 묶어 구우면 옛 폴더가 남는데, 다시 굽기 전에는 아직 쓰이는 폴더라 미리 지울 수 없었다 | 01 세트 절 · `sets_check_cli` [5] |
| 2-8 | **장비(무기) 툴 — 구조 합의 + 런타임 발판**: 파트 z 간격 10(`DRRigModel.Z_STEP`, 장비가 파트 **사이**에 낄 자리) · `DREquipItem.z_after_part`("이 파트 바로 앞", 퍼펫이 매 프레임 따라감) · `DREquipSet` + `DRPuppetSet.equip()/unequip()`(모든 세트에 한 번에 · 무기 교체 · reload 뒤 재장착). ⚠ **그 전에 구운 세트는 z 간격이 1** — 무기를 파트 사이에 끼우려면 다시 구워야 한다 | 사용자 "모델 2D화는 거의 끝났으니 무기를 얹고 싶다 · 조준하는 손에 피벗 · 퍼펫의 순서 데이터를 불러와 몇 번째에 그릴지 그 툴에서 · 프리뷰" (무기 교체·무기 안의 움직임 있음) | 01 §4-27 · `puppet_set_check_cli` [5] |
| 2-9 | **장비(무기) 굽기 코어** — `core/equip_baker.gd`(`DREquipBaker`) + `tools/equip_bake_cli.gd` + `DREquipSet.load_json()`. 세트마다 같은 카메라·같은 레스트로 무기를 손 뼈에 붙여 무기만 찍음 → `res://equip/<id>/<세트>.png` + `equip.json`. 그립은 소총을 든 세트의 레스트에서 자동(오른손 → 왼손 = 총열 방향). **Kar98k 를 지금 구워진 세트 3개에 구워 넣음**(`res://equip/kar98k/`, 원본 `res://source3d/weapons/kar98k/` — 색 텍스처 1k 만). 시험장 `E` 키 = 장비 바꿔 들기. ⚠ 세트를 다시 구우면(이름·레스트가 바뀌므로) 장비도 다시 구워야 한다. 창(그립을 눈으로 보며 고치는 곳)은 아직 없음 | 사용자 "장비 피벗 맞추는 건 어딨어 에디터툴?" — 아직 없어서 코어부터 | 01 §4-27 |
| 3 | 프리셋 `composites` · `composite_upper_parts` / `rig.json` `composites` / CLI `--composites="이름=하체+상체;…"` | 정의가 저장돼야 다시 구울 수 있다 | — |
| 0 | **픽셀 퍼펙트 = 선의 색 방식** — 상단 바 `카툰` / `픽셀 퍼펙트` 토글(같은 ButtonGroup, 하나만 켜짐) + `%`(어둡게 하는 정도). 픽셀 퍼펙트는 선의 도트마다 **가장 가까운 몸 도트 색 × (1 − %)** 로 칠한다 — 주황 살 옆은 짙은 주황, 보라 관절 옆은 짙은 보라. 셰이더 uniform `outline_tone` · `outline_tone_strength`, 프리셋·cfg `outline_style` · `outline_tone`, rig.json 같은 키, CLI `--outline_style=pixel_perfect --outline_tone=0.45`. 전체 실루엣/파트별, 장비 밑깔개에도 그대로 적용 | 사용자 "내가 아는 픽셀 퍼펙트는 이 기법이 아니다 — 색감에 맞게 그 결의 톤으로 보정돼 들어가는 것, 카툰 아웃라인과 같이 적용될 수 없으니 토글이어야" | 01 §4-25 · 02 M4 |
| 4 | **도트 격자 고정**(처음에 "픽셀 퍼펙트"로 잘못 알아듣고 만든 것 — 이름을 바꿔 남김) — 상단 바 체크. 프리뷰: 2D 퍼펫을 배율 1 로 그리고 정수 픽셀에 붙임(부드러운 도트 이동 칸은 잠김). 베이크: **`puppet_pixel.tscn`**(`DRPixelPuppet` 래퍼 = 작은 SubViewport 에 그려 정수 배 확대) + 움직임 범위 자동 계산 | 그냥 확대하면 파트가 돌 때 도트가 기울어진다. 선 색 방식과는 별개라 같이 켜도 된다 | 01 §4-24 |
| 5 | 새 런타임 `runtime/dr_pixel_puppet.gd` — `pixel_scale`(정수) · `extra_margin` · `play()` · `get_puppet()` · `get_animation_player()` | 게임에 바로 넣을 수 있게 | README "도트 격자 고정으로 게임에 넣기" |
| 6 | 프리셋 `pixel_grid` / `rig.json` `pixel_grid` · `pixel_view` / `sets.json` `pixel_scene` / CLI `--pixel_grid` | — | — |
| 7 | 상단 바를 HFlowContainer 로 · 팝업의 줄바꿈 라벨에 최소 너비 | 체크박스 하나로 오른쪽 열이 창 밖으로 밀렸고(스모크 16), 팝업이 세로 5,487px 로 떴다(캡처로 발견) | 02 U13 |
| 8 | 새 검사 `tools/composite_check_cli.gd` · `tools/pixel_check_cli.gd` · 스모크 32·33 | — | README ⑪⑫ |

## 실측

| | 값 |
|---|---|
| **픽셀 퍼펙트** — 선 도트 875개(정측면 · 선 1도트) | 카툰: 전부 검정 · 픽셀 퍼펙트: 검정 **0**, **옆 몸 도트 × 0.55 와 98% 일치**(나머지는 두 색이 만나는 모서리의 평균) · 캐릭터 안쪽 바뀐 픽셀 0 |
| 도트 격자 고정 — 3배로 그렸을 때 3×3 칸이 한 색이 아닌 칸 (96px · Jog_Fwd·Roll 12프레임) | 래퍼 **0 / 16,811** · 그냥 `puppet.tscn` 을 3배 **1,106 / 1,703** |
| 도트 격자 고정 — 움직임 범위 | 캔버스 96×96 → 뷰포트 102×102, 가장자리에 닿은 픽셀 0 |
| 상하체 합성 — 뼈 자세 비교(다리·힙 = 하체 애니, 팔·몸통 = 상체 애니) | 72개 중 어긋남 0 |
| `Crouch_Idle + Idle` 의 몸통 숙임(힙 → 목이 수직에서 앞으로 기운 각도) | Idle 7.4° · 원본 Crouch_Idle 49.2° · 방향 유지 0% 30.2° · 50% 21.6° · **100% 12.8°**(가슴 방향은 Idle 과 0.04° 차이) · 100% + 보정 +20° → −2.8° |
| `Crouch_Fwd + Rifle_Aiming_Idle` | 4.00초 = 하체 2.00초 × 2번, 상체 3.10 → 4.00초(속도 ×0.78). 3D·2D 모두 자세 정상 |

## 검증 (옛 사본 `Documents\1941` — 코드 동일)

| 도구 | 결과 |
|---|---|
| `ui_smoke_cli` | 전부 통과 — 34 선 색 방식(카툰/픽셀 퍼펙트 토글 · 선 색 · 프리셋 왕복) · 32 도트 격자 고정 · 33 상하체 합성 팝업 · 16 배치 · 1-1 툴팁 42개 |
| `composite_check_cli` · `pixel_check_cli` | 전부 통과 |
| `retarget_check` · `sets_check` · `planar_check` · `anim_merge_check` · `layer_check` · `rootbone_check` · `drift_check` · `equip_test` | 전부 통과 |
| 실제 프로젝트(`SideProject`) | 에디터가 꺼져 있을 때 `--import` + 문법 검사 통과(새 클래스 `DRCompositeDialog` · `DRPixelPuppet` 등록, `.uid` 생성) |

## 구운 결과를 게임처럼 돌려 보기 (Unity 와의 대응)

| Unity | 여기 |
|---|---|
| Animator 가 붙은 프리팹 | 세트 1개의 `puppet.tscn` (그 안의 AnimationPlayer 에 그 세트의 동작들) |
| Animator 안의 클립 | 세트에 담은 동작 |
| 프리팹을 씬에 놓고 `animator.Play("X")` | `DRPuppetSet` 노드에 `sets.json` 을 주고 `play("X")` — 세트(폴더)가 여러 개여도 이 노드 하나가 묶는다 |

- 시험: `res://test/puppet_test.tscn` 을 열고 **F6**. `1`~`9` 동작 바꾸기 · 마우스 조준(조준 동작일 때 자동) · `A` 조준 자동/켬/끔 · `F` 좌우 반전 · `+`/`−` 확대 · `L` 반복 · `R` 다시 구운 뒤 다시 읽기.
- ⚠ 지금 `puppet/` 은 **세트 3개에 동작이 1개씩**이다(`Idle` / `Crouch_Idle+Idle` / `Rifle_Aiming_Idle`). 그래서 씬이 3개로 나뉘어 있다.
  `Idle` 과 `Rifle_Aiming_Idle` 은 같은 서기 계열이니 **한 세트에 같이**(5번에서 Ctrl 클릭으로 둘 다 고르고 `세트로 추가` 한 번) 굽는 게 맞다 —
  그래야 그 세트의 `puppet.tscn` 하나에 동작 2개가 들어가고, 둘 사이를 오갈 때 파트 그림이 안 바뀐다. 앉기는 자세 계열이 달라 따로 세트.
  (`puppet/Sprint` · `puppet/Walk` 는 예전에 구운 폴더가 남은 것이라 09-21 에 지웠다 — `sets.json` 에 없고 다른 데서 참조도 없었다. 그 뒤로는 세트 굽기가 목록에서 빠진 세트의 폴더를 알아서 치운다 — 위 2-7.)
- **`puppet/` 은 결과물 폴더다 — 손으로 관리하는 곳이 아니다.** 관리하는 건 ① 3D 원본(`source3d/`) ② 프리셋 `.tres`(파트 · 그리기 순서 · 합성 · 세트 목록) ③ 게임 쪽의 `DRPuppetSet` 노드 하나. `puppet/` 안을 손으로 고치면 다시 구울 때 덮어써진다.

## 알아 둘 것
- **픽셀 퍼펙트(선 색)는 아직 바깥 테두리와 파트 경계에만** 들어간다. 몸 안쪽 경계(팔이 몸통 앞을 지나는 곳)의 톤 선이나 볼록한 모서리의 밝은 선은 없다 — 안쪽 선에 가장 가까운 건 `전체 실루엣` 을 끄고(파트별) 픽셀 퍼펙트를 켜는 것.
- **도트 격자 고정과 부드러운 도트 이동은 반대 성격**이다. 도트 격자 고정은 격자가 완벽한 대신 느린 동작에서 도트가 톡톡 튀고, 부드러운 이동은 매끄러운 대신 확대하면 경계가 섞인다. 둘 다 구워 보고 고를 것.
- 게임 전체를 저해상도로 그리는 프로젝트(스트레치 모드 `viewport`)라면 `puppet_pixel.tscn` 없이 `puppet.tscn` 을 배율 1 로 쓰면 된다.
- 합성 동작은 조합 수만큼 구운 애니가 늘어난다. 너무 많아지면 게임 쪽 AnimationTree(Blend2 + 필터) 방식으로 — 인수인계 README 남은 일 10.
- (오늘 발견, 애드온과 무관) `project.godot` 의 메인 씬이 09-16 에 지운 옛 `puppet/puppet.tscn` 을 가리킨다 → F5 가 메인 씬을 다시 고르라고 한다. 02 에 적어 둠.

### 파일
- 새: `addons/dot_rigger/ui/composite_dialog.gd` · `runtime/dr_pixel_puppet.gd` · `runtime/dr_puppet_set.gd` · `runtime/dr_equip_set.gd` · `core/equip_baker.gd` · `tools/equip_bake_cli.gd` · `equip/kar98k/` · `source3d/weapons/kar98k/` · `tools/composite_check_cli.gd` · `tools/pixel_check_cli.gd` · `tools/puppet_set_check_cli.gd` (+ 각 `.uid`) · `test/puppet_test.tscn` · `test/puppet_test.gd` · `Docs/인수인계/img/` · 이 문서
- 수정: `core/baker.gd` · `core/exporter.gd` · `core/set_baker.gd` · `core/preset.gd` · `runtime/smooth_pixel.gdshader`(선 색 방식) · `ui/main_window.gd` · `tools/bake_cli.gd` · `tools/sets_check_cli.gd` · `tools/ui_smoke_cli.gd` · 문서 4편
