# Dot Rigger — 버그 기록과 교훈

> 작성 2026-09-14, 갱신 2026-09-15. 날짜 표시가 없는 항목은 전부 **2026-09-09** 작업 중 발견·수정.
> "발견" 칸이 **사용자**인 것은 자동 테스트가 못 잡은 것들이다 — 비슷한 기능을 추가할 때 같은 류를 의심할 것.

---

## 0. 기존 결과물 유효성

아래 **C1(자세 드리프트)** 과 **C3(힙 루트 본)** 은 구운 결과 자체를 틀리게 만든다. 둘 다 **2026-09-09 17:36** 에 수정 완료.

| 결과물 | 구운 시각 | 판정 |
|---|---|---|
| 그 이전에 구운 모든 출력 | ~09-09 17:36 | **무효 — 다시 구울 것** |
| 사용자 `puppet/` (+ `UAL1_preset.tres`) | 09-11 16:26 | 유효 (두 수정 이후) |
| **pitch ≠ 0** 으로 구운 결과 | ~09-15 | **무효 — 위아래가 반대였음** (C8) |
| 09-15 오후 발가락을 따로 꺾어 구운 결과 (17파트, 사용자 `puppet/` 15:30·15:34 포함) | 09-15 | 발가락이 2~4px 로 꿈틀거림(P6). 기본이 "발에 합침"으로 바뀌었으니 **다시 구울 것** (15파트, 발 늘이기 없음) |
| 도트 셰이더 수정(C9) 전에 구운 모든 파트 그림 (사용자 `puppet/` 16:20 포함) | ~09-15 저녁 | 겹치는 부위(손 안쪽·몸통 뒤 어깨·관절 안쪽)가 틀리게 찍힘. **다시 구울 것** |

> 같은 이유로 그 이전에 잰 **3D 대비 불일치 수치와 거기서 나온 각도 조언도 무효** — M1 참고.

---

## 1. 치명 — 구운 결과가 틀리게 나오던 것

### C1. 파트를 찍는 동안 자세가 흘러감
- **발견**: 사용자 "2D 순서 체크하면 왜 재생이 멈추냐" → 역추적해 보니 3D 프리뷰가 **의도치 않게 계속 재생 중**이었음
- **원인**: `set_pose()` 가 `play()` 만 하고 세우지 않음. 파트 15장을 한 장씩(프레임 2개씩 대기) 찍는 사이 자세가 계속 진행
- **실측**: 30프레임 사이 재생 위치 0.033 → 0.605, 같은 파트를 시간차로 두 번 렌더하면 91픽셀 다름
- **해결**: setup 에서 `speed_scale = 0`. 프리뷰 재생만 `set_playback(true)`
- **검사**: `tools/drift_check_cli.gd`

### C2. `pause()` 가 바인드 포즈로 되돌림 (C1 첫 수정의 부작용)
- **발견**: drift 검사에 "서로 다른 자세가 실제로 다르게 찍히는가"를 추가해서
- **원인**: `pause()` 하면 믹서가 자세 적용을 멈춰 스켈레톤이 레스트로 돌아감
- **해결**: `pause()` 금지, speed_scale 0 방식
- **덤으로 틀렸던 것**: 검사 기준으로 `stop()` 을 "바인드 포즈"라 가정했는데 `stop()` 은 **마지막 자세를 그대로 둔다**. 비교 대상을 Idle@0 / Idle@50% / A_TPose 로 바꿈 (814px / 3468px 차이로 정상 확인)

### C3. 힙 루트 본이 `root` — 힙·상체가 고정
- **발견**: 사용자 "스프린트 재생하면 힙하고 상체가 막 고정돼서 안 맞는다". 초기 베이크 때부터 Hips 피벗 y=185(192 캔버스의 발바닥)가 이상했으나 넘어갔었음
- **원인**: 파트 안 최상위 본을 루트로 잡음 → UE 리그 `root` = **정점 웨이트 없음, Jog 중 이동·회전 0.0000**. Torso 는 Hips 의 자식이라 같이 어긋남
- **해결**: 루트 본 후보를 `weighted_bones`(정점을 하나라도 지배하는 본)로 제한 → `pelvis` (Jog 중 이동 0.1214)
- **검사**: `tools/rootbone_check_cli.gd`
- **교훈**: 수치가 이상하면(피벗이 발바닥) 그때 파고들 것

### C4. 회전 ±360° 튐 → 엔진이 키 사이를 한 바퀴 헛돔
- **발견**: 투영값 분석 (`L_Hand −360.4 ~ 5.9`, `R_Foot −367.8 ~ 13.2`)
- **원인**: `atan2` 는 [−π, π] 로 감김. 파이썬 재구성은 프레임을 독립 계산해서 멀쩡해 보였고, **보간하는 엔진에서만** 드러남
- **해결**: `_unwrap_rotations()` + 루프 애니 끝에 0프레임 사본(`_closing_frame`)

### C5. 베이크된 씬의 정지 상태에 z_index 가 없음
- **발견**: 사용자 "yaw −45 로 봤는데 오른쪽 어깨가 몸통에 가려져야 하는데 레이어가 이상"
- **원인**: z 를 애니 트랙에만 넣고 씬엔 안 넣음 → 전부 0 → 씬 트리 순서(팔이 몸통보다 나중)대로 그려짐
- **해결**: `resolve_z_order()` 로 레스트 깊이 순번을 Sprite2D 에 박음. 수동 순서면 그걸로
- **검사 방법**: 3D 합성과 `preview_cli --static` 을 나란히 놓고 차이 맵

### C6. 해상도를 바꿔도 SubViewport 크기가 그대로
- **발견**: 사용자 "도트화에서 해상도 줄였더니 프리뷰 위치가 찐빠"
- **원인**: 뷰포트 크기는 setup 때 한 번만 설정. 자동 맞춤은 UI 의 새 해상도로 중심·배율을 계산 → 어긋남. **베이크에도 같은 문제**
- **해결**: `apply_view_size()` 를 프리뷰 갱신과 베이크 시작 때 호출
- **검사**: 스모크 15번 (192/96/64/256px 에서 중심 오차 ≤0.5px)

### C7. 팔 단축 미보정
- **발견**: 러닝 사이클 재구성에서 팔이 관절에서 떨어져 보임 + 단축률 진단(팔 0.69~1.05)
- **해결**: stretch 노드(근사). 베이크마다 `[단축률 진단]` 출력

### C8. (2026-09-15) Pitch 부호가 반대 — "탑다운"·"아이소" 프리셋이 아래에서 올려다봄
- **발견**: 회전 기즈모의 드래그 방향을 정하려고 잰 값. `pitch = +60` 에서 카메라가 모델 중심보다 **6.4 아래**
- **원인**: `Basis.from_euler` 의 X 회전은 양수일 때 +Z 쪽 점을 아래로 내린다. 그대로 쓰면 +pitch = 아래에서 올려다봄
- **해결**: `DRBaker.view_basis()` 에서 pitch 부호를 뒤집음 → 양수 = 위에서 내려다봄. 사용자 프리셋(pitch 0)은 영향 없음
- **검사**: 스모크 20번 (pitch +60 → 카메라가 모델보다 위)
- **교훈**: "탑다운" 같은 이름표를 달았으면 그 이름대로 보이는지 한 번은 수치로 확인할 것 — 서 있는 인물은 위아래가 반대여도 눈에 잘 안 띈다

### C9. (2026-09-15) 3D 렌더에서 겹친 부분이 비쳐 보임 — 파트 그림까지 틀림
- **발견**: 사용자 "3D 일 때 항상 겹쳐진 부분이 통과돼서 보인다"
- **원인**: `shaders/dot_flat.gdshader` 가 `ALPHA = base.a;` 를 썼다. Godot 4 는 셰이더가 ALPHA 를 쓰기만 해도(값이 1 이어도) 반투명 파이프라인으로 보내고, 반투명 물체는 깊이를 기록하지 않는다 → 픽셀마다 앞뒤를 가리지 못하고 **나중에 그린 면이 위에 올라간다**. 뒤쪽 어깨가 가슴 위로, 엉덩이 관절 안쪽 보라색 면이 허벅지 위로 비쳤다.
- **측정** (`puppet_test/depth_probe.gd`, yaw −55° · 184px, 같은 자세에서 셰이더만 바꿔 비교): 전체 합성 Idle **6.0%** · Jog_Fwd **7.8%** 픽셀이 달랐고, **파트 단독 그림도** 오른손 93px 중 18px · 몸통 1053px 중 38px 가 틀렸다.
- **해결**: `ALPHA_SCISSOR_THRESHOLD = 0.5;` 추가 → 불투명 파이프라인(깊이 기록) + 투명 텍스처는 0.5 기준 오려내기. 알파를 아예 뺀 셰이더와 합성·파트 단독 모두 **0px 차이**.
- **영향**: 수정 전의 3D 프리뷰 · 구운 파트 PNG · `compare_cli` 수치 전부. 3D 대비 불일치 재측정: 정측면 9.4% · −45° 3.9% · −55° 7.8% · −65° 11.4% (09-14 의 4.3~5.9% 를 대체, M1 참고). 결과물은 다시 구울 것(§0).
- **교훈**: 기준("3D = 정답")으로 쓰던 것 자체가 틀려 있었다 — 비교의 기준부터 검증할 것. Godot 셰이더에서 ALPHA 를 쓰면 반투명 취급이므로, 불투명으로 그리려면 알파 가위를 같이 준다.

### M1. (2026-09-14) 드리프트가 비교 측정까지 오염 → 틀린 결론을 사용자에게 전달
- **발견**: 인수인계 검증에서 사용자 각도 −55° 불일치가 4.3% 로 나와 이전 수치(12~22%)와 안 맞음
- **원인**: 각도별 불일치를 C1 수정 **전**에 쟀다. 비교도 파트를 한 장씩 찍으므로 자세가 흘러 차이가 부풀려짐. 같은 도구·같은 각도로 −65° 가 22.2% → 5.5%
- **틀렸던 결론**: "정측면이 3⁄4 보다 확실히 유리" · "−65° 는 불리" · "뭉친 곳은 손·전완" → 실제로는 각도별 4.3~5.9% 로 차이가 작고, 뭉치는 곳은 사타구니
- **09-15 추가**: 이 4.3~5.9% 도 3D 기준이 틀린(C9 — 셰이더가 깊이를 안 씀) 상태에서 잰 값이었다. 셰이더 수정 후 같은 도구로 재측정: 정측면 9.4% · −45° 3.9% · −55° 7.8% · −65° 11.4% — 각도 차이가 다시 커졌다. **비교의 기준(3D)이 맞는지부터 확인할 것.**
- **교훈**: 렌더 경로 버그를 고치면 **그 전에 잰 수치와 거기서 나온 결론을 전부 다시 잴 것**

### (설정 함정) T포즈를 레스트로 쓰면 측면에서 팔이 뭉개짐
- 코드 버그 아님. 측면에서 팔이 카메라를 향해 `UpperArm 13×13`, `Forearm 10×11`(고리 모양). Idle 로 바꾸면 `15×36`
- 대응: 레스트 드롭다운에서 `idle*` 자동 선택 + 경고 문구

---

## 2. 2D 프리뷰 / 재생

### P1. 3D 프리뷰가 수동 그리기 순서를 무시
- **발견**: 사용자 "R_Forearm 누르는 순간 지정한 정렬이 흐트러진다" → "클릭 방식에 따라 미리보기 정렬만 이상하다". **화면 캡처로 확인**: 목록 순서는 멀쩡, 프리뷰가 3D 라 순서가 반영될 방법이 없었음
- **해결**: `2D 순서` 모드. `▲▼` 첫 입력 시 자동 전환

### P2. 2D 순서 모드에서 재생 불가
- **발견**: 사용자 "그니까 왜 2D 순서 체크하면 재생이 안 되냐고"
- **원인**: 처음엔 파트 이미지 캐시를 겹치기만 하는 구조라 막아 둠. **원리적 한계가 아니었다** — 쉬운 구현을 택했던 것
- **해결**: 캐시 파트를 베이크 씬과 같은 `bone → stretch → art` 구조에 붙이고 `project_local()` 로 움직임

### P3. 재생 중 파트 캐시를 찍다가 팔다리가 몸에서 떨어짐
- **발견**: P2 구현 직후 캡처 확인
- **원인**: `set_process(true)` 후 캐시 렌더(await) 사이에 `_process` 가 자세를 바꿈 = C1 과 같은 종류
- **해결**: `_process` 는 `_busy` 동안 아무것도 안 함
- **검사**: 스모크 "레스트에서 2D 퍼펫 vs 3D 실루엣 차이" (수정 후 0.0%)

### P4. `▲▼` 를 눌러도 프리뷰가 안 바뀜
- `_move_z()` 끝에 `_refresh_preview()` 누락

### P5. (2026-09-15) 2D 로 재생하면 찌글찌글 — 옛날 TV 노이즈 같음 → **부드러운 도트 이동**으로 대응
- **발견**: 사용자 "2D 로 재생하면 찌글찌글, 옛날 티비 노이즈 같다"
- **1차 측정**: Jog_Fwd · yaw −55° · 184px, 1/60초 간격 30장. 지표 = 연속 3장에서 A→B→A 로 되돌아가는 픽셀(실제 움직임은 한쪽으로 진행하므로 되돌아가는 픽셀이 곧 노이즈)

  | 방식 | 깜빡임 (캐릭터 대비) | 프레임당 바뀐 픽셀 |
  |---|---|---|
  | 3D 렌더 | 5.0% | 2911 |
  | **2D 퍼펫 (지금)** | **7.9%** | 2657 |
  | 2D + 픽셀 스냅 | 9.4% | 2786 |
  | 2D + 늘이기 끔 | 7.5% | 2678 |
  | 몸 전체를 0.2°씩만 회전 | 0.1% | 252 |
  | **2D + 12fps 계단 재생** | **0%** | 1229 |

- **원인**: 60fps 로 매 프레임 파트들이 서로 다른 방향으로 1픽셀 미만씩 움직이면, 테두리·명암 경계 픽셀이 샘플 격자를 넘나들며 켜졌다 꺼졌다 한다. 3D 렌더도 같은 이유로 5% 가 깜빡이고, 2D 는 **이미 도트로 뽑은 조각을 다시 돌리고 옮기며 한 번 더 뽑기** 때문에 더 심하다. 회전 자체(0.1%)·늘이기(7.5%)는 주범이 아니고, 픽셀 스냅은 위치만 정수로 끊어 오히려 튄다.
- **기각한 대응 — 계단 재생**: 포즈를 잡고 1/12초마다 바꾸면 깜빡임은 0 이지만(같은 포즈가 여러 프레임 유지되니 원리상 0), 사용자 "부드러운 도트 애니메이션이 좋은 거지 완전 비트 게임을 원하는 게 아니라 지글거린 것만 고쳐지면 된다" → 기각.
- **2차 측정** (`puppet_test/jitter_probe2.gd` + 파이썬 분석, 저장소 밖 임시. 1/60초 × 40장, 전부 화면 배율 ×4 크기로 맞춰 비교). 1차 지표(되돌이 픽셀 수)로는 방식 차이가 안 잡혀서 지표를 추가했다 — **흐린튐** = 도트 2칸 크기로 흐리게 본 뒤 앞뒤 프레임 평균에서 벗어난 정도(떨어져서 보이는 노이즈), **움직임** = 프레임 간 변화량(부드러움이 유지되는지).

  | 방식 | Idle 흐린튐 | Idle 움직임 | Jog_Fwd 흐린튐 |
  |---|---|---|---|
  | a 지금 (184px 캔버스에 그린 뒤 확대) | 25.5 | 19.4 | 343.1 |
  | b 화면 배율로 그림 (nearest) | 21.0 | 20.2 | 337.2 |
  | c b + 경계만 섞는 셰이더 (텍스처 필터로 섞음) | 6.8 | 19.7 | 331.0 |
  | **c2 c + 알파를 미리 곱해 섞기** | **4.9** | **19.3** | **332.0** |
  | d 12fps 계단 (참고) | 23.6 | 16.3 | 290.5 |
  | e 3D 렌더 (참고) | 23.3 | 15.9 | 330.7 |
  | f 실제 툴 경로 (옵션 켬, 프리뷰 400%) | 4.9 | 19.3 | — |

- **해결** (`runtime/smooth_pixel.gdshader`, 창 `6. 내보낼 애니메이션` 의 `부드러운 도트 이동`(단축 보정 아래) 기본 켬): 2D 퍼펫을 **화면에 보이는 배율 그대로** 그리고(최대 ×8), 파트 스프라이트에 "텍셀 안쪽은 또렷하게, 텍셀 경계만 화면 1픽셀 폭으로 섞는" 셰이더를 붙인다. 베이크 씬의 `art` 스프라이트(머티리얼 1개 공유)와 `DRPuppet.equip()` 장비 스프라이트도 같은 머티리얼. `rig.json` 에 `"smooth_pixel": true`.
- **교훈 1 — 확대만으로도, 셰이더만으로도 안 된다**: b(화면 배율 + nearest)는 a 와 수치가 거의 같다. 셰이더는 섞을 화면 픽셀이 있어야 의미가 있어서, 184px 캔버스에 그린 뒤 확대하면 섞을 곳이 없다 → 둘 다 필요. 프리뷰 확대 계산이 텍스처 크기 대신 **원래 크기(`_pv_logical`)** 를 쓰게 바꾼 이유(안 그러면 뷰포트가 커질 때마다 "해상도 바뀜"으로 보고 확대가 풀린다).
- **교훈 2 — 필터로 섞으면 테두리에 어두운 선**: c 는 투명 텍셀의 RGB(검정)가 섞여 파트 테두리·관절(목, 허벅지)에 1px 어두운 선이 보였다. c2 는 `texelFetch` 로 4텍셀을 읽어 알파를 미리 곱한 뒤 섞는다(텍스처 필터·임포트 설정과 무관).
- **교훈 3 — 지표**: "되돌이 픽셀 수"는 부드럽게 섞인 경계의 옅은 변화까지 똑같이 세서 c2 가 오히려 높게 나온다(Idle 0.02% → 0.13%). 비교 그림으로 보면 a 는 **몸통 안쪽에 점이 흩뿌려져** 깜빡이고 c2 는 **경계선을 따라 이어지는** 옅은 변화다. 눈에 보이는 노이즈는 흐린튐으로 판단할 것.
- **한계**: 뛰기처럼 빠른 동작에서 남는 변화(흐린튐 332)는 3D 렌더(331)와 같은 수준 = 동작 자체라 이 방식으로 더 줄지 않는다. 게임이 캐릭터를 **확대해서** 그릴 때만 효과가 있다(1:1 로 그리면 nearest 와 거의 같음).
- **검사**: 스모크 21번(뷰포트 = 캔버스 × 화면 배율, 셰이더 17개, 확대해도 "맞춤" 유지, 캔버스 크기로 줄이면 끈 상태와 실루엣 차이 1.8%) · 스모크 19번 프리셋 왕복에 `smooth` · `equip_test_cli` 장비 머티리얼 = 본체 머티리얼.

### P6. (2026-09-15) 도트화해도 발가락이 너무 섬세하게 움직여서 어설픔 → **발가락을 발에 합치고 발은 한 장·발목 회전만**
- **발견**: 사용자 "도트화 해도 발가락이 너무 섬세하게 움직여서 어설프다". 같은 날 오전 발가락 분리(사용자 요청 "걸을 때 발이 접히니 발가락 본도 파츠 나눠 달라")의 결과.
- **측정** (사용자 결과물 yaw −30° · 184px · BackFlip 12fps, 같은 각도로 60fps 재굽기):
  - 발가락 길이가 화면에서 **2~4픽셀** (60fps 재굽기에서는 1.6~2.7px).
  - 키의 **68~77% 가 1픽셀 미만** 움직임(중앙값 0.05~0.11px), 23키 동안 꺾임 방향이 **8~10번** 바뀜 → 꿈틀거림.
  - 단축 보정 배율이 **0.62 ↔ 1.60**(제한 끝까지) 튐. 길이가 2~4px 라 투영 길이가 조금만 흔들려도 비율이 크게 변한다.
- **검토한 대응 — 발가락 회전을 30° 단위로 끊기**: 60fps 데이터로 계산하면 자세 변화 초당 4~6회·경계 되돌이 0~1회였지만, 사용자가 "발은 항상 레스트 모양 그대로, 발목만 움직이고 발가락은 접히든 말든"을 제시해 적용하지 않음.
- **해결** (`core/part_profile.gd` `humanoid(split_toes = false)`):
  - `ball|toe` 본을 **Foot 파트에 합친다**. 발 이미지는 레스트 자세 그대로 한 장.
  - Foot 규칙 `"stretch": false` → `DRRigModel.no_stretch` → `project_local()` 의 `s` 가 항상 1. 2D 프리뷰와 베이크가 같이 따르고, 베이크 씬에는 발 늘이기 트랙을 만들지 않는다. `rig.json` parts 에 `"stretch"`.
  - 발 방향은 원래부터 **루트 본(foot) 공간에 고정된 tail 점**으로 재므로(01 §4-6), 발가락 뼈가 접혀도 발 그림은 따라 접히지 않고 발목 회전만 반영된다.
  - `humanoid(true)` / `bake_cli --split_toes` 로 발가락 분리 모드를 쓸 수 있다(레이어 기능은 그대로).
- **결과**: 15파트. 사용자 각도(−30°) Jog_Fwd·BackFlip 전 프레임 발 늘이기 1.00, 발 2D 회전 폭 64~168°.
- **한계**: 걸을 때 발가락이 접히는 모습은 표현하지 않는다.
- **검사**: `layer_check_cli` [0] 기본 프로필(15파트, ball→Foot, 루트 본 foot, 늘이기 끔, Jog_Fwd 24프레임 s = 1, 씬에 발가락 본·발 늘이기 트랙 없음, rig.json stretch) + [1]~[4] 분리 모드 · 스모크 8-1.

### P7. (2026-09-15) Idle·Walk 를 구웠는데 AnimationPlayer 에 마지막에 구운 것 하나뿐 → **같은 폴더에 구우면 애니가 쌓이게**
- **발견**: 사용자 "idle 로 레스트 포즈 해놓고 idle, walk 둘 다 구웠는데 애니메이션 플레이어에 마지막 구운 거 한 개". 원하는 것 = 한 레스트 포즈 기준으로 여러 번 구우면 목록이 여러 개. (그 전에 사용자는 5번 레스트 포즈를 "구울 애니 고르는 칸"으로 알고 있었음 — 5번 = 조각 찍는 자세, 6번 = AnimationPlayer 에 넣을 동작)
- **원인 1**: 베이크가 씬과 rig.json 을 **이번에 고른 애니만으로** 새로 만들었다 → 같은 폴더에 따로 구우면 앞의 애니가 사라짐. (사용자 `puppet/` 16:20 = Idle 하나)
- **원인 2**: 6번 검색칸을 바꾸면 `_fill_anim_list()` 가 목록을 지우고 다시 채우며 **선택을 복원하지 않았다**. 베이크는 목록에 보이는 항목의 선택만 읽어서, "walk" 로 찾아 고른 뒤 "idle" 로 찾아 고르면 Idle 만 구워졌다.
- **내 실수**: 같은 날 툴팁을 달면서 검색칸에 "선택은 유지됩니다"라고 **코드 확인 없이** 적었다(사실과 반대). 이번 수정으로 사실이 됐다. **툴팁·문서 문구도 동작을 확인한 뒤 쓸 것.**
- **해결**:
  - 창: 고른 애니를 `_picked_anims`(이름 집합)에 따로 기억한다(목록 클릭 → `_sync_picked_from_list`). 목록을 다시 채울 때 복원하고, 베이크·프리셋도 이 집합을 쓴다. 목록 아래 `선택 N개: …` + `선택 비우기`.
  - `DRExporter.keep_previous`(창 `같은 폴더의 기존 애니 유지` 기본 켬 · 프리셋 `keep_anims` · CLI `--fresh` 로 끔): 저장 전에 예전 rig.json 의 애니를 이어 담는다. 같은 이름은 이번 것으로 교체.
  - 섞어도 되는지 `_incompatible_reason()` 이 본다 — 애니 값은 레스트 포즈를 이 카메라로 찍은 기준의 상대값이라 **레스트 포즈(이름·시점%) · 해상도 · Ortho · 카메라 기저/위치 · 파트 이름 목록**이 같아야 한다. 다르면 예전 애니를 빼고 이유를 남긴다(`dropped_reason`, 창 상태줄에 ⚠).
- **검사**: `tools/anim_merge_check_cli.gd` (Idle → Jog_Fwd 추가로 2개 → Idle 다시 구워도 2개(교체) → 레스트를 바꾸면 예전 애니 빠짐 + 이유 → 유지 끄면 이번 것만) · 스모크 7-1(검색을 바꿔도 선택 유지) · 19(프리셋 `keep_anims`).

---

## 3. UI

### U1. Shift 다중 선택이 곧바로 풀림
- **발견**: 사용자 "쉬프트로 눌러도 다중으로 안 잡힌다"
- **원인**: 프리뷰 갱신마다 `_fill_z_list()` 가 `clear()` 후 **첫 선택 하나만** 복원
- **해결**: 순서가 그대로면 목록을 안 건드림, 바뀌면 이름으로 전부 복원
- **사용자 요청 반영**: 선택한 파트들**만** 프리뷰에 표시

### U2. 파트를 누르면 들어가서 못 나옴
- **발견**: 사용자 "힙 눌렀는데 힙으로 들어와 버렸다. 루트 전체로 되돌아가는 건?"
- **해결**: `◀ 전체 보기` 버튼 + 파트 트리 맨 위 `＊ 전체 합성` 행. `_show_composite()` 는 조기 반환 없이 위젯 상태를 직접 맞춤

### U3. 코드를 고쳐도 창에 반영 안 됨 + 에러 폭탄
- **발견**: 사용자 "툴이 에러가 많이 난다" / "창이 꺼지면 그냥 false 가 되는 것 같네"
- **원인**: 창 X = `hide()`. `plugin.gd` 는 인스턴스가 살아 있으면 재사용 → 옛 구조 인스턴스에 새 코드가 얹힘. `@tool` 핫리로드는 구조 변경을 못 따라감
- **해결**: `프로젝트 > 도구 > Dot Rigger — 창 새로 만들기 (초기화)`. 그래도 안 되면 에디터 재시작

### U4. 파일 탐색기를 열면 툴 창이 뒤로 밀림
- **발견**: 사용자 "모델 찾기 누르면 고도 파일탐색기가 뜨고 툴이 아래로 내려간다"
- **원인**: 다이얼로그를 `EditorInterface.get_base_control()`(메인 에디터 창)에 붙임
- **해결**: 툴 창(`self`)의 자식으로. 모델·프리셋 저장·불러오기가 `_file_dialog()` 하나를 공유

### U5. 에디터 밖에서 파일 대화상자 크래시
- **원인**: `EditorFileDialog.new()` 는 에디터 밖에서 **null**(엔진 로그 `Class type: 'EditorFileDialog' is not instantiable`)
- **해결**: null 이면 `FileDialog` 로. CLI 테스트에 이 로그가 찍히는 건 정상

### U6. 레스트 포즈에 "Idle" 대신 "대기"
- **발견**: 사용자 화면 캡처
- **원인**: 에디터가 Control 텍스트를 자기 번역 사전으로 자동 번역
- **해결**: 창에 `auto_translate_mode = AUTO_TRANSLATE_MODE_DISABLED`. (내부적으론 원본 문자열이라 기능 영향은 없었음 — 캐릭터가 Idle 자세였던 게 증거)

### U7. 슬라이더를 돌리면 프리뷰가 낡은 상태로 남음
- **원인**: 갱신 중 들어온 요청을 `return` 으로 버림
- **해결**: `_dirty` 로 합쳐 끝나고 한 번 더

### U8. 오른쪽 그리기 순서 열이 창 밖으로 쫓겨남 (2번)
- **발견**: **창 캡처 + 위젯 좌표 검사**(스모크 16번)
- (a) 줄바꿈 없는 상태 Label 이 텍스트 전체 너비(559px)를 최소 너비로 요구 → `clip_text = true`. 단 **clip_text 만 주면 높이가 1px 로 찌부러짐** → `custom_minimum_size.y = 22`
- (b) `▶ 재생` 추가로 툴바 한 줄 폭 초과 → 툴바(확대·2D·재생)와 내비(전체 보기·상태)를 두 줄로
- **교훈**: UI 를 건드리면 스모크 16번(창 밖/너무 작음/목록 스크롤)을 반드시 돌릴 것

### U9. 여백이 픽셀 고정이라 저해상도에서 손해
- 여백 6px 기준 세로 채움: 256px 96% · 192px 94% · 96px 88% · 64px 81%
- 대응: 상태줄에 `채움 %` 표시. **비례 조정은 보류**(도트는 "몇 픽셀"이 예측 가능한 게 낫다고 판단, 사용자 결정 대기)

### U10. Bone2D 말단 본 경고
- `No Bone2D children ... Cannot calculate bone length` → 길이·각도를 3D 값으로 직접 지정

### U11. (2026-09-14) 새로 clone 한 PC 에서 스모크 테스트 실패 가능
- **원인**: 테스트가 `res://puppet_test/` 에 13군데 파일을 쓰는데 폴더를 안 만듦. 그 폴더는 `.gitignore` 대상. 19번 프리셋 저장 검사는 실패로 처리
- **해결**: 테스트 시작 시 폴더 생성

---

## 4. 스크립트 작성 중 오류 (GDScript / 도구)

| # | 증상 | 원인 → 해결 |
|---|---|---|
| S1 | `The function signature doesn't match the parent` | 헬퍼 이름 `_get` 이 Object 가상함수와 충돌 → `_arg` |
| S2 | 구운 씬에 `DRPuppet` 스크립트가 안 붙음 | `dr_puppet.gd` 가 아직 등록 안 된 `DREquipItem` 을 참조 → 로드 실패. **새 `class_name` 추가 후엔 `--import` 먼저** |
| S3 | `There is already a variable named "sig"/"bad"` | 긴 함수에서 같은 스코프 변수명 재사용 → 이름 변경 |
| S4 | `Cannot infer the type of "path"` | 람다 파라미터는 Variant 라 `:=` 불가 → `var path: String = String(p)` |
| S5 | `unexpected EOF while looking for matching '` | 긴 GDScript 를 bash heredoc 으로 쓰다 **백슬래시 줄바꿈**이 bash 를 깨뜨림 → Write 툴 / 파이썬 치환 |
| S6 | (피해 없음) | Git Bash `sed -i` 를 한 번 사용 — 이 환경 금지 규칙 위반. 이후 Edit 툴·파이썬만 |
| S7 | `unexpected EOF while looking for matching '` (09-15, 두 번째) | S5 의 진짜 원인은 bash heredoc **본문 안의 작은따옴표**. 파이썬 패치를 heredoc 으로 넘기다 통째로 실패(파일은 하나도 안 바뀜). 본문에 큰따옴표만 쓴 heredoc 은 괜찮았다 → 스크립트는 Write 로 **파일에 저장한 뒤** `python 파일` |

---

## 5. Godot · GDScript 함정 요약 — 다음에 또 밟을 것들

**실행 / 렌더링**
- `--headless` 는 더미 렌더러라 **SubViewport 가 안 그려진다**. 렌더가 필요한 CLI 는 창을 띄워서(`--resolution 400x300`) 돌린다. `bake_cli --diag` 만 headless 가능
- 출력을 받으려면 `Godot_..._console.exe`
- `viewport.get_texture().get_image()` 전에 `await RenderingServer.frame_post_draw` 두 번. 대기 없이 다시 읽으면 검은 이미지
- **숨긴 Window 안의 SubViewport 는 렌더되지 않는다**(검은 프리뷰). 베이크 중 창 닫기 금지
- 새 `class_name` 스크립트를 추가하면 `--headless --import` 로 전역 클래스 등록부터

**애니메이션**
- `pause()` → 바인드 포즈로 돌아감 / `stop()` → 마지막 자세 유지 / 고정은 `speed_scale = 0`
- 재생 중인 AnimationPlayer 가 있으면 `await` 사이에 자세가 바뀐다. 여러 장을 한 자세로 찍을 땐 반드시 고정
- glTF 임포트는 `_Loop` 접미사를 떼고 loop_mode 로 바꿈
- z_index 값 트랙은 `UPDATE_DISCRETE` + `INTERPOLATION_NEAREST`
- 각도 트랙은 프레임 순서대로 펴서(unwrap) 넣을 것

**에디터 플러그인 / UI**
- `@tool` 핫리로드는 멤버 변수 추가 같은 구조 변경을 못 따라감
- 에디터는 툴 UI 텍스트를 자동 번역함 → 데이터 표시 창은 `auto_translate_mode` 끄기
- `EditorFileDialog` 는 에디터 밖에서 null. 다이얼로그는 **띄운 창의 자식**으로
- 줄바꿈 없는 Label 은 텍스트 폭이 최소 너비 → 컨테이너 전체를 밀어냄. `clip_text` 는 최소 높이까지 0 으로 만듦
- `ItemList` 다중 선택: `select_mode = SELECT_MULTI`, 신호는 `multi_selected`, 추가 선택은 `select(i, false)`
- 중첩된 CanvasItem 에서 z 를 절대값으로 쓰려면 `z_as_relative = false`
- 컨테이너 안에서 자식 위치를 직접 잡으려면(확대·이동) 맨 `Control` 을 부모로

**GDScript**
- Object 가상함수 이름(`_get`, `_set` 등)을 헬퍼로 쓰지 말 것
- 람다 파라미터는 Variant — 타입 명시
- `a if cond else b` 를 줄바꿈하려면 `\` (이건 GDScript 에선 정상. bash heredoc 에선 문제 — S5)

---

## 6. 이 작업 환경의 규칙

- Git Bash **`sed -i` 금지** (대용량 파일이 0바이트가 된 전력)
- PowerShell `Get-Content → -replace → Set-Content` 로 **한글 파일 치환 금지** (CP949 로 읽혀 통째로 깨짐). 치환은 Edit 툴 또는 UTF-8 지정 파이썬
- 긴 스크립트는 bash heredoc 대신 Write 툴로 파일에 저장해 실행. 특히 **heredoc 본문에 작은따옴표가 있으면 명령 전체가 깨진다** (S5·S7)
- 사용자 **화면 캡처**는 확인받은 뒤에만. PowerShell `CopyFromScreen` 으로 전체(여러 모니터 이어붙임, 이 PC 는 6400×1584 · 오른쪽 모니터 x=3840부터) → 필요한 부분만 잘라 보고 **원본 캡처 파일은 삭제**
- Claude 메모리는 PC 마다 따로다. **이 문서가 원본**

---

## 7. 측정값 모음 (근거)

| 항목 | 값 |
|---|---|
| UAL1 매핑 | 65본 → **15파트**(발가락은 발에 합침, 09-15 P6), 미매핑 0 — 발가락 분리 모드(`humanoid(true)`)는 17파트 · 순서 목록 15줄 |
| 3D 렌더 대비 2D 순서 합성 불일치 (`compare_cli`, 192px, Idle 정지, 자동 순서, **09-15 C9 셰이더 수정 후**) | 정측면 90° **9.4%** · −45° **3.9%** · −55° **7.8%** · −65° **11.4%** |
| ~~09-14 측정~~ (3D 가 깊이를 안 써서 기준 자체가 틀림, C9) | 정측면 4.6% · −45° 5.9% (링0 도 5.9%) · −55° 4.3% · −65° 5.5% |
| 남는 불일치가 뭉치는 곳 | 3⁄4(−45°·−65°) = **사타구니**(허벅지가 Hips 앞 조각을 덮음) + 손 약간 · 정측면 = 몸통 뒤쪽 팔과 몸통의 경계선 (09-14 기준 — C9 수정 후 재확인 안 함) |
| ~~드리프트 수정 전 측정~~ (무효, M1) | 정측면 12.4% · −45° 14.6% · −65° 22.2% |
| 단축률 (측면, Jog_Fwd) | 다리·몸통·머리 0.99~1.02 · 팔 0.69~1.05 · R_Foot 1.30 (Idle 레스트에서 발이 바깥으로 틀어짐) |
| 관절 겹침 링 (L_Calf) | 크기 18×50 / 18×53 / 18×57 · 발목 겹침 15px(링0) / 106px(링2) |
| 해상도별 세로 채움 (여백 6px) | 256 96% · 192 94% · 96 88% · 64 81% |
| 자세 드리프트 (수정 전 → 후) | 30프레임에 0.033→0.605, 91px → 0.000, 0px |
| 힙 루트 본 (수정 전 → 후) | root 이동 0.0000 → pelvis 0.1214 |
| 장비 부착 | 36×22 헬멧 1장이 Jog_Fwd 전 프레임 머리에 고정 |
| 프리뷰 확대 앵커 | 맞춤 162% → 800%, 커서 아래 지점 어긋남 ≤1.5px, 위치 정수 스냅 |
| 발가락 (UAL1) | `ball_l` 정점 27개(발 78) · 파트 삼각형 71(발 202) · 접히는 애니 69/120, 최대 기어가기 71.6° · `Jog_Fwd` 레스트 대비 최대 38.3°, 2D 발 기준 회전 폭(최대−최소) 약 51° |
| 발가락 움직임 (P6, 분리 모드, yaw −30° · 184px) | 길이 2~4px · 키의 68~77% 가 1px 미만 · 23키에 방향 전환 8~10회 · 늘이기 0.62~1.60 → 발에 합친 뒤 발 늘이기 1.00 고정 |
| 2D 재생 깜빡임 (P5 1차, 되돌이 픽셀) | 3D 5.0% · 2D 7.9% · 스냅 9.4% · 늘이기 끔 7.5% · 0.2° 회전만 0.1% · 12fps 계단 0% |
| 2D 재생 흐린튐 (P5 2차, 화면 배율 ×4) | Idle: 지금 25.5 → 부드러운 도트 이동 **4.9** (움직임 19.4 → 19.3 그대로) · Jog_Fwd: 343 → 332 (3D 렌더 331) |
| pitch 부호 (수정 전, C8) | pitch +60 에서 카메라가 모델 중심보다 6.4 아래 |
