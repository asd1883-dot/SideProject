# Dot Rigger 작업 내역 — 2026-09-15

> 커밋용 요약. 자세한 설계·버그 기록은 `Docs/인수인계/` (01 설계 · 02 버그와 교훈 · README), 사용법은 `addons/dot_rigger/README.md`.
> 이 날 작업은 처음에 옛 사본 `Documents\1941` 에서 하다가, 에디터가 여는 실제 프로젝트가 이 저장소(`git\Godot\SideProject`)인 걸 확인하고 전부 옮겼다. 두 폴더 모두 프로젝트 이름이 "1941" 이라 창 제목으로 구분이 안 된다.

---

## 커밋 전후에 할 일

1. **Godot 재시작** — 핵심 스크립트(파트 프로필·리그·베이커·창)가 바뀌었다. 열려 있던 창에는 새 UI 가 안 붙는다.
2. **`puppet/` 다시 굽기** — 지금 결과물은 셰이더 수정 전 그림이라 겹치는 부위가 틀리고, 애니도 Idle 하나뿐이다.
   순서: 시점 확정 → 6번에서 서기 계열 동작(Idle·Walk·Jog…)을 고름 → 5번 **`6번 동작에서 찾기`** → 베이크. 같은 폴더면 애니가 쌓인다.
   Crawl·Swim 계열은 5번 레스트를 그 계열 애니로 두고 **`이 애니에서 자세 찾기`**, 6번에는 그 계열만 → **다른 폴더**(예: `res://puppet_crawl`)에 베이크.
3. `puppet/parts/L_Toe.png`, `R_Toe.png`(+ `.import`) 는 발가락 분리 때 남은 파일 — 다시 구운 뒤 지워도 된다.
4. `addons/dot_rigger/tools/anim_merge_check_cli.gd` 는 새 파일이라 `.uid` 가 없다. 에디터를 한 번 열면 생긴다(같이 커밋).
5. `puppet_test/` 가 이 저장소에서는 git 에 올라가 있고 `.gitignore` 에 없다. 검증 도구를 여기서 돌리면 그 파일들이 바뀐다 — 추적에서 뺄지 결정 필요.
6. `UAL1_preset.tres` 의 변경(그리기 순서·레스트 `Idle_LookAround`·애니 `BackFlip`)은 사용자가 에디터에서 저장한 것이다.

---

## 바뀐 것

| # | 내용 | 왜 | 기록 |
|---|---|---|---|
| 1 | **3D 회전 기즈모** — 3D 프리뷰 오른쪽 위. 드래그 = 좌우·상하 회전, 축 끝 클릭 = 그 방향에서 보기. `2. 시점` Yaw·Pitch 칸과 연동(칸이 기준) | 숫자 입력만으로는 각도 잡기가 불편 | 01 §4-16 |
| 2 | **Pitch 부호 수정** — 양수 = 위에서 내려다봄 | 탑다운·아이소 버튼이 아래에서 올려다보고 있었음 | 02 C8 |
| 3 | **부드러운 도트 이동**(기본 켬) — 2D 퍼펫을 화면 배율로 그리고, 도트 칸 경계만 섞는 셰이더(`runtime/smooth_pixel.gdshader`)를 파트·장비 스프라이트에 붙임 | 2D 재생이 옛날 TV 노이즈처럼 찌글거림. 계단 재생(12fps 끊기)은 사용자가 기각 | 02 P5, 01 §4-17 |
| 4 | **툴팁 전 항목** — 입력칸뿐 아니라 이름 글자에 올려도 뜸(`_tip()`) | Unity 인스펙터처럼 설명이 필요 | 01 §4-12 |
| 5 | **발가락은 발에 합침**(기본) — 발은 레스트 모양 그대로 한 장·발목 회전만·늘이기 없음. `humanoid(true)` / `--split_toes` 로 발가락 분리 모드 | 오전에 발가락을 따로 꺾었더니 184px 에서 2~4px 발가락이 꿈틀거려 어설픔 | 02 P6, 01 §4-15 |
| 6 | 파트별 늘이기 끄기 — 프로필 규칙 `"stretch": false` → `rig.no_stretch` | 발처럼 짧은 파트는 단축 비율이 크게 튐 | 01 §4-8 |
| 7 | **3D 겹침 비침 수정** — `dot_flat.gdshader` 에 `ALPHA_SCISSOR_THRESHOLD` | `ALPHA` 만 쓰면 반투명 취급돼 깊이를 안 써서 뒤쪽 면이 앞으로 비침. **파트 그림도 틀리게 찍혔음** | 02 C9 |
| 8 | **같은 폴더에 구우면 애니가 쌓임** — 7번 `같은 폴더의 기존 애니 유지`(기본 켬). 레스트 포즈·시점·해상도·파트가 다르면 섞지 않고 이유 표시. CLI `--fresh` | 따로 구우면 마지막 것 하나만 남았음 | 02 P7 |
| 9 | **6번 검색을 바꿔도 선택 유지** + `선택 N개` 표시 + `선택 비우기` | 검색을 바꾸면 선택이 풀려 마지막 검색 것만 구워졌음 | 02 P7 |
| 10 | **캐릭터 이동 X/Y**(px) — 도트 크기는 그대로 틀만 이동. CLI `--offset_x/--offset_y` | 수영 자세처럼 몸이 틀 밖으로 나가 잘림(자동 맞춤 끔일 때). 해상도로는 안 넓어짐 | 01 §4-16 |
| 11 | **▶ 재생 애니 드롭다운** — 6번에서 고른 애니 중 골라 재생, 레스트 포즈는 그대로 | 수영을 보려고 레스트를 바꾸면 파트 그림까지 수영 자세로 찍혔음 | 01 §4-11 |
| 12 | 인수인계 문서·README 갱신, 저장소 경로를 이 저장소로 정리 | 다른 PC 작업용 | Docs/인수인계/README |
| 13 | **레스트 자세 찾기** 버튼 2개(5번) — `이 애니에서`(애니 고정, 시점만) / `6번 동작에서`(애니가 바뀔 수 있음). 이 각도에서 팔다리가 화면과 가장 나란한 프레임을 레스트로 | −55° Idle 은 오른발이 카메라를 향해 Walk 의 종아리·발목이 꺾여 보였음. Walk 25% 레스트면 3D 대비 14% → 5% | 02 P8, 01 §4-18 |
| 14 | 발 늘이기 다시 켬(5번 항목 되돌림) | 끄면 짧게 찍힌 발이 못 길어져 더 어긋남(실측) | 02 P8 |
| 15 | **레스트 시점 슬라이더가 베이크에 안 가던 버그** 수정 | 프리뷰는 슬라이더 시점, 베이크는 항상 0% 였음 | 02 C10 |
| 16 | 자세 그룹 워크플로 문서화 — Crawl·Swim 은 그 계열 프레임을 레스트로 **다른 폴더**에 굽기 | 서 있는 그림으로는 엎드린 몸을 못 그림(원리적 한계). Crawl_Enter 16% 레스트면 15~26% → 13~17% | 02 P8, README |

### 파일

- 코어: `addons/dot_rigger/core/baker.gd` · `exporter.gd` · `part_profile.gd` · `preset.gd` · `rig_model.gd`
- 셰이더: `addons/dot_rigger/shaders/dot_flat.gdshader` · **새** `addons/dot_rigger/runtime/smooth_pixel.gdshader` (+ `.uid`)
- 런타임: `addons/dot_rigger/runtime/dr_puppet.gd` (장비도 본체와 같은 머티리얼)
- 창: `addons/dot_rigger/ui/main_window.gd`
- 검증 도구: `tools/bake_cli.gd` · `compare_cli.gd` · `equip_test_cli.gd` · `ui_smoke_cli.gd` · **새** `layer_check_cli.gd` (+ `.uid`) · **새** `anim_merge_check_cli.gd`
- 문서: `addons/dot_rigger/README.md` · `Docs/인수인계/01_설계와_구조.md` · `02_버그와_교훈.md` · `README.md` · 이 파일

### 프리셋(.tres)에 새로 생긴 값
`offset_x`, `offset_y`(시점) · `smooth_pixel`(포즈/애니) · `keep_anims`(출력). 옛 프리셋은 기본값(0 / 켬 / 켬)으로 열린다.

### rig.json 에 새로 생긴 값
`view.view_offset` · `parts[].layer` · `parts[].stretch` · `layer_order` · `smooth_pixel`

---

## 검증 (코드가 같은 옛 사본 `Documents\1941` 에서 실행 — 이 저장소의 추적 파일 `puppet_test/` 를 안 건드리려고)

| 도구 | 결과 |
|---|---|
| `ui_smoke_cli` | 전부 통과 — 1-1 툴팁(25개, 이름 글자에도) · 7-1 검색 바꿔도 선택 유지 · 8-1 15파트 · 19 프리셋 왕복 · 20 기즈모 · 21 부드러운 도트 · 22 캐릭터 이동 (−10, −12) · 23 재생 애니 분리 · 16 창 배치 |
| `layer_check_cli` | 전부 통과 — [0] 기본: 발가락 합침·발 늘이기 1.0 / [1]~[4] 발가락 분리 모드 |
| `anim_merge_check_cli` | 전부 통과 — 쌓임 · 같은 이름 교체 · 레스트 다르면 빠짐 · 유지 끄면 이번 것만 |
| `rootbone_check_cli` / `drift_check_cli` | 정상 / 포즈 고정 OK |
| `equip_test_cli` | 장비 머티리얼 = 본체 머티리얼 |
| 3D 겹침 측정 | 수정 전 합성 6.0~7.8% 픽셀이 잘못 가려짐 → 수정 후 0 |

## 측정값

| 항목 | 값 |
|---|---|
| 2D 재생 노이즈(흐린튐, Idle) | 25.5 → 부드러운 도트 이동 **4.9** (움직임 양 그대로) |
| 발가락(분리 모드, yaw −30° · 184px) | 길이 2~4px, 키의 68~77% 가 1px 미만, 늘이기 0.62~1.60 → 합친 뒤 발 늘이기 1.00 |
| 3D 대비 2D 순서 불일치 (192px, Idle, C9 수정 후) | 정측면 9.4% · **−45° 3.9%** · −55° 7.8% · −65° 11.4% (09-14 의 4.3~5.9% 는 틀린 기준) |
| 기본 틀(자동 맞춤 끔·Ortho 0) 밖으로 나가는 애니 | 120개 중 20개 (Swim·Death·BackFlip·Crawl 계열) |
| 공중제비 BackFlip, yaw −38° vs 90° | 몸통 0.49배·머리 0.57배로 눌림(종이 접힘) vs 1.00배 |
| Walk −55° 512px, 3D 대비 실루엣 차이 | 레스트 Idle 0%: 9~14% (R_Calf 35% · R_Foot 55~64%) → 레스트 **Walk 25%** + 발 늘이기: **5~6%** |
| Crawl_Fwd −55° | 레스트 Idle: 15~26%(덩어리) → 레스트 Crawl_Enter 16%: 13~17%(기어가는 사람) |
| 레스트 단축률(−55°, 가장 눌린 파트) | Idle 0.41(R_Foot) · A_TPose 0.56(팔) · Walk 83% 0.89 · Crawl_Enter 16% 0.83 · Swim/BackFlip 계열 ≤0.77 |

## 사용자에게 설명한 것 (문서에도 반영)

- **5번 레스트 포즈** = 파트 그림(조각)을 찍는 자세 하나. **6번 내보낼 애니메이션** = `puppet.tscn` AnimationPlayer 에 들어갈 동작. 5번에서 고른 애니는 동작으로 들어가지 않는다.
- 각도는 **동작이 화면 안에서 일어나는 방향**으로 고른다(걷기·공중제비는 측면). 몸이 카메라 쪽으로 넘어오면 종이가 접히듯 보인다.
- Head 가 Torso 아래인 건 모델 뼈대(pelvis → spine → neck → head)를 따른 것 — 몸통이 움직이면 머리·팔이 따라온다.
- Ortho 0 = 모델 크기 × 1.15 자동. 자동 맞춤이 켜지면 시작값일 뿐.
