# Dot Rigger 작업 내역 — 2026-09-18

> 커밋용 요약. 앞의 것은 `작업내역_2026-09-16.md`. 자세한 기록은 `Docs/인수인계/`(01 §4-22 · 02 M3 · README 남은 일 10~12), 사용법은 `addons/dot_rigger/README.md` "다른 출처의 동작 얹기 (Mixamo)".

## 한 줄 요약
**Mixamo 에서 받은 소총 동작 7개를 지금 쓰는 UAL 캐릭터에 얹어 Dot Rigger 로 구울 수 있게 됐다.** 전부 Godot 안에서 된다(Unity 의 Humanoid = Godot 의 BoneMap).

## 커밋 전후에 할 일

1. **Godot 재시작** — 베이커·창·프리셋 스크립트가 바뀌었다. 다시 켜면 `source3d/` 의 새 파일들을 Godot 가 가져온다(UAL 21MB 라 1~2분).
   끝나면 파일시스템 독에서 `source3d/mixamo/Firing_Rifle.fbx` 를 더블클릭해 Skeleton3D → Retarget 에 `bonemap_mixamo.tres` 가 들어가 있는지 한 번 확인.
2. Dot Rigger 창: **모델 = `res://source3d/UAL1_humanoid.glb`**, **추가 동작 폴더 = `res://source3d/mixamo`** → `모델 불러오기`.
   동작 목록에 기존 120개 + `Firing_Rifle` · `Rifle_Aiming_Idle` · `Prone_Firing_Rifle` … 7개가 같이 뜬다. 프리셋으로 저장해 두면 폴더도 같이 저장된다.
   ⚠ 기존 `UAL1_preset.tres` 는 모델이 `models/UAL1.glb` 다 — 그대로 둬도 되고(기존 작업 안 바뀜), 소총 동작을 쓰려면 모델 칸만 위 경로로 바꿔 새 프리셋으로 저장.
3. `source3d/` 를 커밋할 때 **`.import` 파일도 꼭 같이**(뼈대 맞춤 설정이 거기 들어 있다). ⚠ 저장소가 공개라면 Mixamo 원본 FBX 를 올려도 되는지(Adobe 약관의 재배포 제한) 먼저 확인 — 확인 못 했음.
4. `Firing_Rifle_2` · `Shoot_Rifle` 은 캐릭터가 옆으로 걸어 나간다 — Mixamo 에서 **`In Place` 를 체크하고 다시 받을 것**(그 옵션이 없는 동작이면 제자리 사격용으로는 못 씀).

## 바뀐 것

| # | 내용 | 왜 | 기록 |
|---|---|---|---|
| 1 | **추가 동작 폴더** — 창 `1. 모델` 에 칸 하나(+ 폴더 고르기 버튼). 폴더의 `.fbx/.glb/.tscn/.res` 동작을 같은 캐릭터에 얹는다. **파일 이름 = 동작 이름**. 프리셋 `extra_anim_dir`, CLI `--extra_anims=` | UAL1 에는 소총 동작이 없다(권총뿐). 이 도구는 그림과 동작을 같은 모델에서 읽어서 밖의 동작을 넣을 길이 없었다 | 01 §4-22 |
| 2 | `source3d/` — `UAL1_humanoid.glb`(기존 모델과 같은 파일, 임포트 설정에 BoneMap) · `mixamo/` 7개 · `bonemap_ual.tres` · `bonemap_mixamo.tres` | 뼈대가 다른 동작은 양쪽 본 이름을 표준 휴머노이드로 맞춰 와야 호환된다. **기존 `models/UAL1.glb`·프리셋·구운 결과는 안 건드림** | 01 §4-22 |
| 3 | 파트 규칙에 `little` 추가 | 표준 휴머노이드의 새끼손가락 이름이 `Little` 이라 6본이 손 파트에서 빠졌다 | 01 §4-22 |
| 4 | 새 도구 `tools/retarget_check_cli.gd`(접목 검사) · `tools/make_bonemap_cli.gd`(BoneMap 생성) · 스모크 30번 | 되풀이 검사 · Mixamo 접두사(`mixamorig9` 등)가 다른 파일이 오면 표를 다시 만들 수 있게 | README ⑩ |
| 6 | **`모델 불러오기` 를 다시 눌러도 수동 그리기 순서·고른 동작·레스트 유지**(상태줄에 `· 유지: …`). 그리기 순서는 줄 구성이 같을 때만 | 사용자 "그리기 순서가 초기화되네" — 모델 칸을 바꾸거나 Mixamo 파일을 추가할 때마다 다시 불러와야 하는데 그때마다 순서가 자동으로 돌아갔다. 사용자 프리셋으로 재현·확인(15줄 그대로, 애니 127개) | 02 U12 |
| 7 | **버그 수정 — 레스트 `(바인드 포즈)` 가 "마지막에 보던 프레임"을 찍던 것**. 이제 진짜 바인드 포즈(모델 기본 자세) | 사용자 "바인드 포즈로 해 놓으면 다 맞네? 버그임?" → 버그 맞음. 보던 동작의 프레임이 우연히 레스트가 돼서 잘 맞아 보인 것이고, 베이크에서는 그 순간의 뼈대 자세에 달린 재현 안 되는 결과였다 | 02 C12 |
| 8 | **`지금 프레임을 레스트로`** 버튼(5번 레스트 칸 아래) — 재생 애니 + 위치 슬라이더의 프레임을 레스트로 넣고 자동을 끈다 | 7번 버그로 우연히 되던 "보고 있는 프레임을 레스트로"가 실제로 쓸모 있어서 정식 기능으로 | 02 C12 |
| 5 | 문서 — 애드온 README 새 절 "다른 출처의 동작 얹기 (Mixamo)" · 01 §4-22 · 02 M3 · 인수인계 README(남은 일 10 = 09-17~18 에 합의한 **게임 구조**: 병사≠퍼펫 · 자세 계열별 리그 · 상하체 분리 · 고정할 이름 3가지) | 구조 토론 결과가 대화에만 있었음 | — |

## 실측

| | 값 |
|---|---|
| 표준 뼈대로 다시 가져온 UAL — 기존 동작이 원본과 같은가 (동작 8개 × 4시점 × 본 53개) | 관절 위치 차이 최대: 몸 **4.3mm** · 손가락 포함 4.9mm (184px 캔버스에서 1도트 ≈ 1cm) |
| Mixamo 7개 얹기 | 경고 0 · 파트 15 · 미매핑 본 0 · 엎드려쏴 힙 높이 0.13m(정상) |
| 양손 간격 ÷ 팔 길이 (`Firing_Rifle`) | Mixamo 원본 0.87 → UAL **0.65~0.66**(−25%). UAL 은 어깨가 좁아서. **동작 내내 일정** → 소총이 손 사이에서 안 미끄러진다. 소총 그림을 이 간격(실제 약 36cm)에 맞춰 그릴 것 |

### Mixamo 동작이 "엉망"으로 보인 이유 — 레스트의 자세 계열 (−55° · 192px · 평면화 끔 · 3D 대비 2D 실루엣 차이)

| 레스트 \ 동작 | Firing_Rifle | Rifle_Aiming_Idle | Gunplay | Prone_Firing_Rifle |
|---|---|---|---|---|
| Firing_Rifle 57% (서서 쏴) | 3.2% | 4.4% | 11.7% | **18.4%** |
| Prone_Firing_Rifle 75% (엎드려쏴) | **15.9%** | — | **21.5%** | 2.7% |
| 그 동작 안에서 자동 | 3.2% | 2.1% | 5.6% | 2.2% |

- 접목은 멀쩡하다. **서서 하는 동작과 엎드린 동작을 한 레스트로 같이 보면** 한쪽이 16~22% 어긋난다(Crawl 때와 같은 원리, 02 P8).
  → `Prone_Firing_Rifle` 은 **엎드리기 세트로 따로**(6번 세트), 서서 쏘는 동작끼리 한 세트.
- `Gunplay` · `Double_Dagger_Stab` 은 몸을 크게 비트는 동작이라 같은 계열 레스트여도 6~12%. 평면화를 켜면 24~35% 로 커진다(조준·사격은 켜도 2~3% 그대로).
- `Firing_Rifle_2` · `Shoot_Rifle` 은 캐릭터가 걸어 나가는 동작이라 틀 밖으로 나간다(In Place 로 다시 받을 것).

## 검증 (옛 사본 `Documents\1941` — 코드·재료·경로 동일)

| 도구 | 결과 |
|---|---|
| `retarget_check_cli` | 전부 통과 |
| `ui_smoke_cli` | 전부 통과 — 30 추가 동작 폴더(애니 120 → 121, 공백→`_`, 경고 2종, 자세 = 원본 Walk, 가져온 라이브러리 안 샘, 프리셋 왕복, 다시 불러와도 순서·동작·레스트 유지) · 31 바인드 포즈 = 본 65개 전부 레스트 · `지금 프레임을 레스트로` → Walk 40% · 1-1 툴팁 37개 · 16 배치 |
| `sets_check` · `planar_check` · `anim_merge_check` · `layer_check` · `rootbone_check` · `drift_check` | 전부 통과 |

⚠ **실제 프로젝트에서는 아직 안 돌려 봤다** — 에디터가 이 프로젝트를 열고 있어서 CLI 가져오기를 같이 돌릴 수 없었다. 재료는 옛 사본에서 같은 경로로 만들어 검증한 뒤 `.import` 째 복사했다. 위 "할 일 1·2" 가 실제 확인이다.

### 파일
- 새: `source3d/**` · `addons/dot_rigger/tools/retarget_check_cli.gd` · `tools/make_bonemap_cli.gd` · 이 문서
- 수정: `core/baker.gd`(`extra_anim_dir` · `_load_extra_anims`) · `core/part_profile.gd`(`little`) · `core/preset.gd` · `ui/main_window.gd`(추가 동작 폴더 칸) · `tools/bake_cli.gd`(`--extra_anims`) · `tools/ui_smoke_cli.gd`(30) · 문서 4편
