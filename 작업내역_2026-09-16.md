# Dot Rigger 작업 내역 — 2026-09-16

> 커밋용 요약. 어제 것은 `작업내역_2026-09-15.md`. 자세한 기록은 `Docs/인수인계/`(01 설계 · 02 버그와 교훈 · README), 사용법은 `addons/dot_rigger/README.md`.

## 커밋 전후에 할 일

1. **Godot 재시작** — 창·베이커·익스포터·프리셋 스크립트가 바뀌었고 새 클래스(`DRSetBaker`)가 생겼다.
   ⚠ 오늘 저녁 C11 수정 전에 **세트 2개 이상**을 에디터에서 구운 결과는 앞 세트 씬이 깨져 있다 — 다시 구울 것.
2. 새 파일 `addons/dot_rigger/core/set_baker.gd`, `tools/sets_check_cli.gd` 는 에디터를 한 번 열면 `.uid` 가 생긴다(같이 커밋).
3. 아웃라인을 쓰려면 **다시 구울 것**(파트 그림 여백 + 밑깔개 노드가 새로 들어감). 아웃라인 없이 구운 예전 씬은 그대로 열린다.
   작업 중 잠깐 만들었던 `runtime/outline_whole.gdshader` 는 폐기하고 지웠다 — 에디터가 그 파일을 찾는 경고를 띄우면 무시해도 된다.
4. 5번에서 동작을 고르면 레스트는 자동. 서기/엎드리기/수영 계열은 **6번 세트**로 나눠 담고 베이크 한 번 → `<출력>/<세트>/` + `sets.json`. 게임에서 상태별로 씬을 바꿔 쓴다.

## 바뀐 것

| # | 내용 | 왜 | 기록 |
|---|---|---|---|
| 1 | **재생 속도·스크럽** — ▶ 재생 아래 줄. ×1·×1/2·×1/4·×1/10·멈춤 + 위치 슬라이더. 3D·2D 모두 | 동작을 천천히·프레임 단위로 뜯어보고 싶다는 요청. 멈춤 + `2D 순서` 켜고 끄기 = 같은 프레임의 3D/2D 비교 | 01 §4-11 |
| 2 | **리깅 애니메이션 세트**(6-1) — 5번 레스트 + 6번 동작 한 벌을 이름 붙여 목록에 담음. 추가·불러오기·덮어쓰기·삭제·▲▼, 프리셋 저장. 세트가 있으면 베이크가 전부를 순서대로 `<출력>/<세트>/` 에 굽고 `sets.json` | 자세 계열마다 레스트가 달라야 하는데(어제 P8) 폴더를 손으로 바꿔 가며 굽는 건 번거로움. 사용자 요청 "컨테이너에 담고 삭제·순서 조정, 베이크하면 집합 정보째로" | 01 §4-19 |
| 3 | `DRSetBaker`(새) · `DRExporter.apply_cfg()` — 창의 단일 베이크와 세트 베이크가 같은 설정 통로 | 세트 베이크를 CLI 검사로도 돌리기 위해 창 밖으로 뺌 | 01 §4-19 |
| 4 | (어제 밤) 자세 찾기 버튼을 `이 애니에서` / `6번 동작에서` 둘로 | 레스트를 `Crawl_Bwd` 로 두고 눌렀는데 6번의 `Walk` 로 바뀜 | 02 P8 |
| 5 | **동작 평면화 (2D 게임식)**(6번 아래 체크) — 각도는 측면 포즈 카메라에서 재고(자동 = 게임 yaw 에 가까운 ±90°, 직접 입력 가능) 그림은 게임 시점. 위치 강체·늘이기 1·힙 들썩임만. 프리뷰·베이크·프리셋·CLI `--planar` | "오른손이 z축으로 확 오는 페이퍼맨 느낌" — 3D 를 그대로 투영해 팔이 앞뒤로 접혔음. 2D 게임 컷아웃은 팔다리가 화면 안에서만 돎 | 01 §4-20 |
| 6 | 체크박스 라벨 줄임(`동작 평면화 (2D 게임식)` 등) | 긴 라벨이 왼쪽 열 최소 너비를 키워 오른쪽 열이 창 밖으로 밀림(스모크 16번이 잡음) | — |
| 13 | **도트 아웃라인**(카툰 선) — 창 **상단 바**에 두께(0~3 도트)·색. 파트 그림은 두께만큼 여유를 두고 잘림. CLI `--outline` | 사용자 요청 "카툰 렌더링처럼 검정 아웃라인, 상단 빈 공간에" — 이제 왼쪽·위·가운데·오른쪽·아래 배치 | 01 §4-17 |
| 14 | **아웃라인 두 방식** — 상단 바 `전체 실루엣` 체크(**기본 켬**) = 캐릭터 바깥 테두리에만 선 / 끄면 파트별(관절에도 선). 전체 실루엣은 파트마다 선 색 **밑깔개**(`stretch/outline`, z −1)를 모든 파트 뒤에 깔고 파트가 덮는 방식. 장비도 `equip()` 이 밑깔개를 붙임(장비 그림에 선 두께만큼 투명 여백 필요). 프리셋·cfg `outline_whole`, rig.json `outline_mode`, CLI `--outline_parts` | 사용자 요청 "파츠별과 진짜 전체 아웃라인을 나누고 전체가 기본". 처음엔 CanvasGroup 으로 만들었으나 실험해 보니 z 가 다른 파트가 그룹에서 빠져 폐기 | 01 §4-17 · 02 M2 |
| 12 | 2D 순서 프리뷰를 **캔버스보다 25% 넓게** 그리고 캔버스 틀을 선으로 표시 | 엎드리기·쭈그리기 프리뷰가 레스트 틀에 잘림. 구운 퍼펫에는 틀이 없으니 프리뷰만 넓히면 됨 | 01 §4-11 |
| 11 | **버그 수정 — 세트 2개 이상을 에디터에서 구우면 앞 세트 리그가 깨짐**(C11). 익스포터가 구울 때의 레스트 기하를 붙잡아 두고 `rebuild_scene()` 이 그것만 읽게 | 사용자 "베이크해서 봤는데 리깅이 다 깨져". CLI 는 멀쩡, 에디터의 임포트 후 다시 저장 단계에서 뒷 세트 레스트가 섞였음. `sets_check` [2-1] 로 재현·확인 | 02 C11 |
| 10 | **발 늘이기 ±15% 제한**(프로필 `stretch_limit`) + **손·발 각도 안정화**(`angle_fade` 0.45: 카메라를 향해 단축률이 낮으면 회전을 레스트 쪽으로) | "발이 지렁이처럼 부풀고 휘어진다". −55°·레스트 자동에서는 발 늘이기 0.92~1.14·회전 ±30° 로 크지 않았고, 정면 쪽 각도에서 발이 카메라를 향할 때 각도가 튀는 게 원인 후보 | 01 §4-8 |
| 9 | **세트별 그리기 순서** — 세트에 `z_auto`·`z_order` 저장, 불러오면 오른쪽 목록 복원, 구울 때 세트마다 자기 순서(자동이면 프레임별 깊이). `sets.json` 에 `z_order_manual`·`layer_order` | 사용자 검증 질문: 걷기 세트(순서 A)와 달리기 세트(오른팔을 허벅지 뒤로) — "서로 다른 순서로 구워지나?" → 그때는 **아니었음**(공통, 마지막 목록 상태로 전부). 지금은 세트별 | 01 §4-19 |
| 8 | 왼쪽 열 **접히는 섹션**(제목 클릭, 도트화·파트 분리는 접힌 채 시작, 모델은 불러오면 접힘) + **출력·베이크 버튼·진행바를 스크롤 밖 바닥에 고정** | 섹션이 늘어 스크롤이 길고 베이크 버튼이 맨 아래라 작업할 때마다 내려야 했음 | 01 §4-12 |
| 7 | **5·6번 합침 → `5. 리깅 애니메이션 (내보낼 동작 + 레스트 자세)`** + **`레스트 자세 자동`**(기본 켬): 고른 동작·시점이 바뀌면 알아서 레스트를 찾아 칸에 넣고 칸을 잠금. 세트·프리셋에 `rest_auto`. 6 = 세트, 7 = 출력으로 번호 당김 | 사용자 "레스트와 내보낼 애니가 같은 정보여야 자연스러우니 5·6이 통일됐으면" — 맞는 관찰(02 P8)이라 자동으로 보장 | 01 §4-21 |

### Sprint 레스트 68% 에서 오른발이 휘어 보인다는 건
같은 조건(yaw −55 · 512px · 레스트 Sprint 68% · 발 늘이기 켬)으로 재 보니 오른발의 부모 기준 회전은 **−28°~+19°**, 단축률 0.84~1.0(카메라를 향하지 않음), 3D 대비 전체 실루엣 차이 5~10% — 투영이 튀는 상황은 아니었다. 다만 68% 는 오른발이 **발끝으로 차는 순간**이라 발 그림이 뒤꿈치 든 모양으로 찍힌다. 새 스크럽으로 문제 프레임을 멈춰 `2D 순서` 를 켜고 끄며 3D 와 비교해 보고, 그래도 이상하면 그 화면을 보내 달라고 함. 레스트는 `이 애니에서 자세 찾기`(Sprint 안에서) 또는 Walk 88% 권장.

### 파일
- 새: `addons/dot_rigger/core/set_baker.gd` · `tools/sets_check_cli.gd` · `tools/planar_check_cli.gd`
- 수정: `core/baker.gd`(set_playback 속도 · 포즈 카메라 · project_local/capture_rest 통로) · `core/rig_model.gd`(평면화 계산) · `core/exporter.gd`(apply_cfg · 평면화 호환 검사 · 아웃라인·밑깔개) · `core/preset.gd`(sets · planar · outline) · `runtime/smooth_pixel.gdshader`(아웃라인 · `outline_only`) · `runtime/dr_puppet.gd`(장비 밑깔개) · `tools/bake_cli.gd`(`--planar` · `--outline` · `--outline_parts`) · `tools/equip_test_cli.gd`(장비 밑깔개) · `ui/main_window.gd` · `tools/ui_smoke_cli.gd`(25~29) · 문서 4편

### 프리셋(.tres)에 새로 생긴 값
`sets: Array[Dictionary]` — 각 `{ name, rest_anim, rest_time(0~1), rest_auto, animations, z_auto, z_order }` · `planar` · `pose_auto` · `pose_yaw` · `rest_auto` · `outline_px` · `outline_color` · `outline_whole`(기본 true)
⚠ 옛 프리셋은 `rest_auto` 필드가 없어 **자동으로 열린다** — 저장돼 있던 수동 레스트(예: Idle)는 첫 갱신에서 고른 동작 기준 자동값으로 바뀐다. 수동을 원하면 체크를 끄고 다시 저장.

### 출력에 새로 생긴 것
- 세트 베이크 때 `<출력>/sets.json` — `{ version, base, sets: [ { index, name, dir, scene, rig, rest_anim, rest_time, rest_time_sec, animations, z_order_manual, layer_order, kept, dropped_reason, ok } ] }`
- `rig.json` `view.planar` · `view.pose_yaw` · `outline_px` · `outline_color` · `outline_mode`(whole / parts)
- 전체 실루엣 아웃라인으로 구운 `puppet.tscn` — 파트마다 `Bone2D/stretch/outline`(밑깔개 Sprite2D, z −1). 루트는 예전처럼 Node2D + `DRPuppet`

### 평면화 실측 (−55° · 512px · 레스트 Walk 88% · Jog_Fwd)
| | 3D 대비 실루엣 차이 | 팔다리 |
|---|---|---|
| 3D 투영(끔) | 8~12% | 팔이 앞뒤로 오가며 0.5~1.6배로 접힘(페이퍼맨) |
| **평면화(켬)** | 30~53% (의도된 차이) | 제 길이 그대로 화면 안에서 흔들림 — L_UpperArm 83° · R_Thigh 117° |

## 검증 (코드가 같은 옛 사본 `Documents\1941` 에서)

| 도구 | 결과 |
|---|---|
| `ui_smoke_cli` | 전부 통과 — 25 재생 속도(2D ×1/4 실시간 0.13초에 0.034초 · 멈춤 유지 · 스크럽 · 3D 스크럽) · 26 세트(추가 2 · ▲ · 불러오기 · 덮어쓰기 · 프리셋 왕복 · 삭제) · 29 아웃라인(아래 표) · 18 퍼펫 뷰포트 = CPU 합성(0.0% 다름) |
| `sets_check_cli` | 전부 통과 — `stand/`(Idle·Walk) · `crawl _ test/`(Crawl_Fwd, 레스트 Crawl_Enter 16%) · 몸통 그림 크기 다름 · sets.json 순서 · 아웃라인 두 방식 씬 구조 · 장비 밑깔개 붙고 빠짐 · 루트 Node2D 인 예전 씬에도 스크립트 붙음 |
| `planar_check_cli` | 전부 통과 — 포즈 카메라 −90°·렌더 안 함, 29프레임 전 파트 s = 1, 자식 위치 고정, 힙 들썩임, 회전 폭, 씬 트랙 (1,1), 평면화 끈 애니 섞임 거절 |
| `layer_check` · `anim_merge_check` | 통과 (아웃라인 작업 뒤 다시 돌림) |
| `equip_test_cli` | 통과 — 전체 실루엣으로 구운 씬(장비 밑깔개 = 본체와 같은 머티리얼·z −1) · 예전 씬(밑깔개 없음) |
| `rootbone` · `drift` | 통과 (어제 마지막 실행 기준, 오늘 관련 코드 변경 없음) |

### 아웃라인 실측 (프리뷰, Idle 레스트 · 정측면 · 선 1도트)
| | 검은 픽셀 | 캐릭터 안쪽에서 바뀐 픽셀 |
|---|---|---|
| 선 없음 | 0 | — |
| **전체 실루엣(기본)** | 875 | **0** (바깥 테두리에만) |
| 파트별 | 1157 | 282 (관절 선) |
